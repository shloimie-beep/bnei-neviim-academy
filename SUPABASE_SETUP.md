# Supabase Setup Is Archived

This is not the current BNA database setup guide.

Current BNA operations use Railway hosting plus production Postgres. Do not run
old Supabase SQL against live BNA unless a future migration task explicitly
audits and updates it.

Historical setup notes were moved to:

- `docs/archive/legacy-supabase-setup/SUPABASE_SETUP.md`
- `docs/archive/legacy-supabase-setup/`
- `docs/archive/legacy-family-accountability/`

For current system state, use `README.md`, `MEMORY.md`, `SYSTEM-STATE.md`, and
the server/database bootstrap code that backs `server.js`.
