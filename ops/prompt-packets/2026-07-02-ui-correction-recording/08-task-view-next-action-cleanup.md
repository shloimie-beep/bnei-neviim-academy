# 08 Task View Next Action Cleanup

Status: ready for readback or focused implementation if current code fails.

Scope:

- Verify top task display puts `TASK-20260702-001` or equivalent separate One
  Time Railway target setup first while external setup is blocked.
- Keep internal handoff files out of human-facing task lists.
- Scope One Time task visibility by workspace/project and owner.

Required evidence:

- task-view model/code inspected;
- current top task readback or exact failing selector;
- focused test if code changes are needed.
