# 10 Verifier Deploy Live Smoke

Status: blocked until child implementation packets or external setup readiness
create app-visible changes.

Verifier must check:

- before/after screenshots for app-visible UI work;
- state matrix;
- route/action registry coverage;
- focused tests;
- setup checker;
- deploy/live smoke for app-visible/server-visible changes.

No done status without evidence.
