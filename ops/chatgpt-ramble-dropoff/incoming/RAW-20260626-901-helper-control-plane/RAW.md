Shloimie wants the new Ramble Protocol workflow:

All rambles are done through ChatGPT first. ChatGPT should audit the repo/system context, then write concrete implementation code in a way that saves Codex significant work. ChatGPT should drop the resulting packet/code into a repo-visible drop-off queue. The background agent fleet/Codex should pick it up, apply it, run tests, and report proof through the repo/app.

Immediate payload:
- Apply the BNA helper control-plane code package generated by ChatGPT.
- Build the durable ChatGPT -> Ramble Packet -> Agent Queue -> Codex Run -> Result Record workflow.
- Update AGENTS.md so this is the standard operating model.
- The ChatGPT sandbox path is not a valid agent queue. The repo must own the real intake/drop-off path.
