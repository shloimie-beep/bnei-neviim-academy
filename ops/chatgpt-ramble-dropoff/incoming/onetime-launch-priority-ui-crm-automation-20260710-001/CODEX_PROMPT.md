# Codex Goal-Mode Pickup — One Time Launch Priority

You are Codex in the BNA repository. This packet is trusted operator input, not proof. Audit it against current pushed and live state, then execute safe work instead of producing another terminal audit-only artifact.

## First actions

1. Read BNA-START-HERE.md, AGENTS.md, MEMORY.md, QUALITY-GOALS.md, docs/BNA-RAMBLE-TO-DONE.md, docs/PRODUCT-QUALITY-COMPILER.md, the active execution run, issue #128/comments, and this packet.
2. Refresh `npm run chatgpt:dropoff:tower`, git/current branch, active agent jobs, execution-run status, audit governance, and current One Time deployment truth.
3. Create the next unused raw ID and dated requirement register for this correction. Link earlier sources rather than rewriting them.
4. Separate artifact status from product status. Audit/PQC/prompt completion cannot close a product requirement.
5. Do not stop after the current-state audit. Continue immediately into the first non-overlapping implementation packet and generate repo-visible ChatGPT coding-window packages for the remaining lanes.

## Current-state facts to verify directly

Inspect `public/one-time/index.html` and `config/service-provider-sites/one-time.json` for current placeholder media, noindex/review-only state, review-only footer language, stale copy, plain-text logo placeholders, and unused approved media paths.

Inspect `/api/one-time/interest`, canonical contact upsert code, CRM list/detail APIs, `public/provider.html`, and One Time Operations contacts. Prove whether the returned CRM/contact ID is actually queryable and visible in the UI.

Inspect the mailbox MVP and the source of the separately uploaded roughly 2,600 historical emails. Do not treat the earlier 9-email Resend backfill or small thread count as the complete import.

Inspect current helper naming and stale Agent Mode prompt contracts. The latest exact public identity is `Robot Scheller` / `Rabbi Scheller's digital assistant`.

## Execution order

### P0-A — landing copy/media/production state

Create a landing copy source-of-truth from prior raw inputs and issue comments. Latest explicit decisions win: free until Rosh Hashanah, Asia/Jerusalem campaign timing, first-party lead capture, and current approved class-link follow-up.

Use already-approved One Time assets where mapped. Missing final choices do not block implementation: create polished image slots and an image-slot manifest, not blank gradient placeholders.

Remove public review/test/internal language. Decide and implement production robots/canonical behavior intentionally. Build a polished campaign with real hierarchy, balanced sections, clean proof strip, and manual screenshot acceptance at 1440/1024/768/430/390.

### P0-B — Robot Scheller

Implement exact title and subtitle. Add an approved face/avatar slot and accessible WhatsApp icon. Keep the greeting and choices concise. Do not show a long prohibition list. The assistant must create/update the same canonical CRM contact as the form and log its conversation source/timeline.

### P0-C — form-to-visible-CRM E2E

Create a reversible TEST/synthetic E2E test:

public submit -> returned lead/contact IDs -> authenticated scoped CRM -> find contact -> open detail -> verify source, stage, last activity, and communication timeline.

If backend and UI use different stores or APIs, reconcile them. Telegram notification is not acceptance proof.

### P0/P1-D — CRM workbench

Implement a searchable/filterable compact contact list and stable clickable contact detail/deep-link or split pane. Hide raw tag walls. Show overview, activity, email, WhatsApp, classes/trial, payments/access, notes, tasks/next actions as applicable. Preserve list filters when opening/closing detail. No unrelated BNA data or support diagnostics.

### P1-E — historical inbox reconciliation

Locate the uploaded source, count/dedupe it, identify storage/import state, and determine why the full inbox is not visible. Prepare an idempotent import/thread/contact-link/pagination package when the source is available. Keep private bodies out of repo evidence.

### P0-F — immediate class-link follow-up

Use the runtime-only business WhatsApp bot number ending 8614. Build concise inbound auto-response, CRM upsert, class-interest state, current class-link send decision, idempotency, timeline logging, and visible failure follow-up. Use WhatsApp, email, or both according to available contact data and owner policy.

No bulk campaign. Real activation requires scoped WAPI/Resend readiness, approved concise copy, approved current class-link alias, stop/unsubscribe handling, and one owner-controlled seed test.

### P0/P1-G — Rabbi backend launch readiness

Normal Rabbi pages must not expose irrelevant not-scoped/configured/test/review/fixture/diagnostic/internal-ID language. Hide it or move it to a role-gated support drawer. Every visible control works or has one polished actionable blocked state. Fix uneven grids, off-centered single cards, toolbar inconsistency, and irrelevant Super Admin content. Require manual screenshot review despite zero machine findings.

## ChatGPT coding-window output

Create only the actual unresolved lanes, with dependencies and non-overlapping files. Initial candidate windows:

1. landing copy/media/production-state;
2. Robot Scheller identity/public lead capture;
3. form-to-visible-CRM E2E and canonical contact API;
4. CRM list/detail/timeline UI;
5. historical email import/inbox reconciliation;
6. WAPI/email immediate class-link automation;
7. Rabbi backend launch-readiness cleanup;
8. independent verifier/deploy closeout.

Every GitHub-connected ChatGPT window must create a repo-visible incoming packet with packet.json, RAW.md, CODEX_PROMPT.md, MANIFEST.json, status.json, and actual PATCHES.md diffs/tests. Audit-only prose or another generic prompt is not sufficient.

## Agent Mode sequencing

Do not make the operator run stale prompts first. Update their contracts from this source, implement known gaps, then run Agent Mode as independent live acceptance.

The two pending terminal-proof prompts `rabbi-telegram-helper-ticket-smoke` and `rabbi-helper-tool-scope-map` remain useful, but they do not prove the landing or CRM is launch-ready.

Refresh/create post-implementation prompts for landing/media, Robot Scheller, form-to-CRM E2E, contact detail, historical inbox, inbound class-link automation, Rabbi backend launch readiness, and final manual visual acceptance.

## Completion rule

Do not call this launch-ready while any of these remain true:

- landing still looks like a review/placeholder page;
- submitted lead cannot be found and opened in CRM;
- contact email/WhatsApp history is fragmented or absent;
- expected historical inbox source is unaccounted for;
- immediate class-link path is neither implemented nor precisely blocked;
- Rabbi normal UI exposes unfinished/internal language or dead controls.

Return exact raw/register IDs, generated packet paths, queue state, commits, deployment/live evidence, manual review paths, and only true operator decisions.
