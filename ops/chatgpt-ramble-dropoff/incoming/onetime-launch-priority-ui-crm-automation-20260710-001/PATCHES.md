# Proposed Patches Or Code

No production code patch is included in this correction packet.

This packet preserves the latest operator source, corrects the priority order, and instructs Codex to generate small GitHub-connected ChatGPT coding-window packages containing actual unified diffs or complete replacement files plus tests.

Codex must not treat the absence of a patch here as permission to stop at another audit. The first safe child implementation lane is the landing copy/media/production-state packet, followed by form-to-visible-CRM proof and CRM contact workbench according to collision/dependency checks.
```

### Codex Instruction

Collect this marked comment with:

```bash
npm run chatgpt:dropoff:comments:apply
npm run chatgpt:dropoff:apply
npm run chatgpt:dropoff:tower
```

Then audit against current repo/live state, allocate a non-colliding raw ID if needed, create the active requirement register, generate real child code packages, and execute the first safe launch-critical batch. Do not mark the underlying product requirements Done merely because this packet was ingested.
