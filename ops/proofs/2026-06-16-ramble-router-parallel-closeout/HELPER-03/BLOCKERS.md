# HELPER-03 Blockers

- Needs Shloimie decision on safe deploy window or isolated helper release path.
- Live `/api/bna/support-tickets` parameter-type issue from the WS05 smoke should be fixed or separately tracked before final closeout.
- No external sends/publishes should be enabled by helper actions without explicit approval gates.
