# Watchdog Audits

This folder stores read-only watchdog reports. Reports must not contain
secrets, private message bodies, or private parent/student/provider data.

Each report should state:

- command run
- timestamp
- goals checked
- findings by severity
- evidence paths
- repair task IDs when created
- blockers when a check cannot run
