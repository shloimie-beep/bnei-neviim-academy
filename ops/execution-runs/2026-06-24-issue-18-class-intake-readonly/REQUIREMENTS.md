# Requirements

Machine-readable requirements live in `requirements.json`.

This run intentionally reuses the existing requirement `REQ-20260624-028`.
Sub-work is tracked as evidence, tests, and acceptance criteria rather than
creating duplicate requirement IDs.

Terminal verdict must be exactly one of:

- `NO CANDIDATES - nothing should be applied`
- `NOT SAFE TO APPLY - reasons listed`
- `READY FOR OWNER APPROVAL - dry run only, no production write performed`
