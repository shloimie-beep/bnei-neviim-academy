# Plan

1. Register the packet and run.
2. Inspect the existing two-week audit artifacts and exporter risks.
3. Document the Drive-first raw storage and GitHub digest-memory policy.
4. Add a safe digest exporter and make the legacy raw exporter refuse default
   runs.
5. Add section classification/routing and focused privacy tests.
6. Generate repo-safe digest memory and sanitized gap/repair/Drive-plan
   artifacts.
7. Capture and verify the issue #41 Drive transcript-library addendum.
8. Run the approved #83-only private Drive transcript-library sync.
9. Record tests, ledger, changelog, and blocker.

Skipped by design:

- No raw transcript-body export.
- No Drive write/move/delete.
- No production DB mutation or class backfill.
- No worker retry or paid retranscription.
- No deploy/live smoke because this run changed local tooling/docs/artifacts
  only.
