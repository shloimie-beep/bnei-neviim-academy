# Final Report

## Summary

Implemented a BNA-side signed One Time subscriber-support consumer and BNA operator Telegram alert queue.

The implementation reuses existing BNA support-ticket records, approval gating, agent-decision metadata, Telegram alert formatting, and action/route registry governance. It does not create a second ticket system and does not broaden Academy/super-admin separation.

## Contract Summary

- Raw-body HMAC signed POST endpoint: `/api/bna/integrations/one-time/support-tickets/v1`.
- Event id and timestamp replay controls.
- Account/product allowlist for `rabbi_sheller_provider / one_time_mishnah_class`.
- Active signed entitlement proof required.
- Durable inbox before processing.
- Lease query uses `FOR UPDATE SKIP LOCKED`.
- Duplicate event and delivery keys use `ON CONFLICT DO NOTHING`.
- Redacted canonical `bna_support_tickets` record.
- Redacted BNA operator Telegram alert queued through `assistant_delivery_outbox`.
- Provider-off reverse-status rows queued through `bna_one_time_support_status_outbox`.
- Retry/dead-letter state uses `retry_wait` and `assistant_dead_letters`.

## Guardrails

- No real Telegram send.
- No Rabbi / One Time bot token.
- No provider action.
- No external send.
- No charge, DNS, deployment, credential, access, or production runtime mutation.
- No automatic Codex task/job/code execution from ticket intake.
- No raw private body returned.
- No attachments accepted.
- No Academy data crossover.

## Verification

Passed:

- `node --check src/lib/bna/one-time-support-consumer.js`
- `node --check server.js`
- `node --check tests/one-time-support-consumer.test.js`
- `node --test tests/one-time-support-consumer.test.js tests/rabbi-telegram-notifications.test.js tests/rabbi-telegram-ticket-approval.test.js` - 28/28
- `node --test tests/watchdog-action-registry.test.js` - 5/5
- `node -e "JSON.parse(...route registry...); JSON.parse(...action registry...)"`
- `npm run bna:run:validate`
- `npm run secrets:audit`
- `npm run pqc:validate`
- `npm run pqc:validate:fixtures`
- `npm run pqc:evals`
- `npm run watchdog:protocol-drift`

The generic `latest-*` PQC and dated drift report files generated by those commands were not kept in this branch because they are not lane-specific evidence.

## Dirty Main Checkout Guardrail

The implementation worktree is the external clean worktree at `C:\Users\User\.overnight-20260717-worktrees\BNA-OPS-02`.

The prompt source was read read-only from the dirty main checkout because the packet was not present in the clean remote-base worktree. No dirty implementation files were copied from the main checkout.

Startup-generated control-tower churn in the clean worktree was removed before commit because it was not lane evidence.

An early patch target hit the original workspace; only those introduced hunks were removed, no staging or commit occurred there, and subsequent implementation used absolute-path edits in the clean external worktree.

## Remaining Blockers

- Production shared secret configuration.
- Provider-side sender implementation.
- Real Telegram dispatch worker for the queued BNA operator alert.
- Signed reverse-status delivery to One Time.
- Live smoke/deploy.
- Draft PR creation is blocked by GitHub auth/permission: `gh auth status` reports an invalid local token, and the GitHub connector returned API 422 `must be a collaborator`. The branch was pushed and the manual PR creation URL is `https://github.com/shloimie-beep/bnei-neviim-academy/pull/new/codex/bna-ops02-onetime-support-consumer`.

All of the above require explicit operator approval and are intentionally not performed in this lane.
