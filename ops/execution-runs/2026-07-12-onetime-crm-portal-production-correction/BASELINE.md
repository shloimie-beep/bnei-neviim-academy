# Baseline

Initial baseline gathered on 2026-07-12:

- `master` is behind `origin/master` by 42 commits.
- Worktree is dirty before this packet. Existing modified/untracked files are treated as another
  lane's work unless explicitly touched by this run.
- Previous active execution run had no unblocked batch and remained blocked; it was marked inactive
  so this run can become the active goal-mode lane.
- `npm run bna:run:status` and `npm run bna:run:next` passed for the previous run and reported no
  next unblocked executable batch.
- `npm run chatgpt:dropoff:tower` reported no ready ChatGPT packet and warned about dirty worktree
  collisions.
- One Time memory confirms workspace `rabbi_sheller_provider`, project `one_time_mishnah_class`,
  brand black/yellow, and no-GHL first-party CRM policy.
- Source screenshot files named in the packet are not present under `/workspace/scratch/...` or
  `C:\workspace\scratch\...`; the attachment folder contains only `pasted-text.txt`.

Initial surface map:

- `ops/surface-maps/2026-07-12-onetime-crm-portal-surface-map.md`
- `ops/surface-maps/2026-07-12-onetime-crm-portal-surface-map.json`
