# Baseline

- Created from `origin/master` at
  `a9528b2d9467174d76d4c25bfb028f9308f24b4f`.
- `origin/master` is the canonical branch. `origin/main` has no merge base
  with `origin/master` and must not be merged blindly.
- The shared checkout `C:\Users\User\BNA v2.0` is dirty and on
  `integration/20260619-platform-finish`; it is audit-only for this run.
- GitHub Issue #7 is open and already contains the prior queued-source comment.
- GitHub Issue #8 is open and had no comments at this run kickoff.
- Service Provider Studio exists on current master with code/tests/live-smoke
  evidence, but Railway deployment metadata remains weaker than endpoint smoke
  proof because `railway:doctor` previously lacked a token in the isolated
  worktree.
