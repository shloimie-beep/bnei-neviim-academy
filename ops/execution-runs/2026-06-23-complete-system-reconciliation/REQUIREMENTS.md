# Requirements

Machine-readable requirements live in `requirements.json`.

This run is a successor to the completed Service Provider Studio run. It
handles the currently safe unblocked reconciliation/containment batch and keeps
external-readback/backfill/deploy decisions explicit rather than claiming false
completion.
