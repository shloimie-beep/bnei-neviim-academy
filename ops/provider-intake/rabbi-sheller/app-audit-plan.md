# App Audit Plan

Audit questions:

- Does the app have parent/member login?
- Does it have student/member login?
- Does it have an admin backend?
- Does it have a video/Vimeo library?
- Does it have class schedule or Zoom links?
- Does it have worksheets/source sheets?
- Does it have communication/dialogue?
- Does it have comments, questions, or threads?
- Does it have moderation?
- Does Rabbi approve or quote student/member questions?
- Does it have payment/access tracking?
- Does it have analytics?
- Does it have CRM/member records?
- Does it have API/export/database access?
- Which design patterns are worth copying?

Outputs:

- Feature map.
- Data model notes.
- Access/security notes.
- Screenshots with no secrets.
- Integration recommendation.
- Keep external / embed / sync / copy-pattern / migrate decision.
