# Baseline Summary

Browser baseline status: blocked.

No 30-sample browser baseline is claimed. This run did not have an approved authenticated local/staging admin, parent, or student session, and no stable browser/database measurement target was available.

Source-backed baseline:

- Base SHA `cebbfc5781b92fcd9a5014df67f8ae4ba0b3a61c` did not have a focused `/operations/school` shell.
- Base SHA did not have `/api/bna/school-admin/summary`.
- School admin work entered through the broader Operations surface.

The after checkpoint uses static source and budget evidence only. Do not claim p50/p75/p95 performance improvement until the browser matrix is collected.
