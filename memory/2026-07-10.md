# 2026-07-10

## LAGSTATE-20260710-001 - OneTime cache/static delivery lane opened

The earlier OneTime app-lag finding remains valid: live evidence showed slow
and variable first-byte timing across HTML, static JS/CSS, and small API
routes, while visual polish defects are tracked separately.

Current correction: the active UI/API collision lane has cleared. A fresh
control-tower readback on 2026-07-10 showed no dirty worktree at sample time,
no ready ChatGPT packets, no claimable agent-fleet jobs, and no active UI
collision lane.

Codex implemented the first conservative cache/static-delivery fix locally:
public JS/CSS assets get a short public cache, public media/fonts get a longer
public cache, and HTML, service worker, manifests, API/private routes, and
Operations shell assets remain no-store/no-cache.

Evidence:

- `server.js`
- `tests/bna-helper-tools.test.js`
- `tests/one-time-intake-api-readback.test.js`
- `tasks-pending/2026-07-09-onetime-parallel-frontend-audit.md`
- `tasks-pending/2026-07-09-production-readiness-goal.md`

Deployment update: commit `4908c905` was pushed and deployed to the OneTime
Railway target `one-time-production / one-time-web` as deployment
`50533728-970f-4936-bc14-bbe439992f6e`, which reached `SUCCESS`.

Live readback:

- `header-readback.md` proves public JS/CSS now use
  `public, max-age=300, must-revalidate`.
- OneTime logo media uses
  `public, max-age=86400, stale-while-revalidate=604800`.
- `/one-time/`, `sw.js`, `manifest.json`, `operations-shell.js`, and
  `operations-deferred-renderers.js` remain no-store/no-cache.
- Fresh no-write lag audit improved from 18/18 samples needing attention to
  1/18. The only remaining threshold finding was parent-review desktop
  `slow_dom_content_loaded`.

Remaining: continue runtime/parent-review performance follow-up only if repeated
samples stay slow; visual polish remains in the separate OneTime frontend audit.

## DEPLOYSTATE-20260710-002 - Main BNA cache policy live

The same static cache policy is also live on `https://bneineviimacademy.org`.
BNA Railway doctor showed service `skillful-motivation` deployment
`b9cd16e1-ac80-4310-9c42-c87541a0343c` reached `SUCCESS`.

Live readback confirmed public JS/CSS short caching, public media longer
caching, and no-store/no-cache for HTML, service worker, manifests, and
Operations shell assets. `npm run app:smoke:public-privacy` passed and
`/api/health` returned OK with database connected.

## LAGSTATE-20260710-003 - OneTime parent-review residual fixed locally

The single remaining lag threshold after the cache deploy was the desktop
`parent-review` route. Codex found that `/parent.html?review=one-time` was
serving the full BNA parent portal shell, about 284 KB, before switching into
OneTime review mode.

Local fix: keep the same canonical route URL but intercept the review query
before static file serving and send a dedicated lightweight
`public/one-time-parent-review.html` shell. Normal BNA parent routes still use
`public/parent.html`.

Local evidence:

- PASS `node --check server.js`.
- PASS `node --test tests/one-time-review-only-server.test.js
  tests/one-time-shared-review-branding.test.js
  tests/one-time-brand-helper-isolation.test.js`.
- PASS local no-write lag audit at
  `ops/performance-audits/2026-07-10-onetime-parent-review-lightweight-local/`
  with 0/18 samples needing attention.
- Parent-review desktop local sample: 25ms DCL, 32ms FCP, 531ms network idle,
  4 requests, 147 DOM nodes, no blockers.

Deployment/live-smoke is still required before calling the residual lag fix
Done.

## LAGSTATE-20260710-004 - OneTime parent-review residual deployed

Commit `9350c43a` deployed to OneTime Railway deployment
`a447201e-f28a-4111-ab59-f6c65ee64e58`, which reached `SUCCESS`. The main BNA
service also serves the same lightweight route from deployment
`105fabb5-63f1-4f6d-9e5f-0221c90fae65`.

Live evidence:

- `https://join.onetimeonetime.com/parent.html?review=one-time` and
  `https://bneineviimacademy.org/parent.html?review=one-time` both return the
  lightweight shell with body length 16886 and `Cache-Control: no-store`.
- Both live route readbacks include OneTime parent review markers and exclude
  the old `data-parent-onboarding-form` and `BNA Parent Portal` title.
- No-write live lag audit passed with 0/18 samples needing attention.
- Parent-review desktop live sample: 829ms DCL, 488ms FCP, 1325ms network idle,
  4 requests, 147 DOM nodes, no blockers.
- Live smokes passed for OneTime separate instance, Rabbi landing, OneTime
  interest dry-run, and BNA public/privacy.

The lag/UI response-time lane is now clear for cache/static delivery and the
parent-review residual. Visual polish remains a separate UI packet, and full
production remains blocked by external setup, Agent Mode terminal proof, and
Rabbi Telegram hosted/live-smoke proof.

## UISTATE-20260710-001 - OneTime static chrome fixed locally

Codex resumed `REQ-20260709-067` after a fresh control-tower/status sample no
longer showed an overlapping active static chrome deploy/edit lane.

Local changes:

- OneTime public/member/library/classroom surfaces now share canonical footer
  coverage, larger OneTime logo treatment, active yellow/black nav state, and
  44px-friendly navigation/footer/classroom/form controls.
- The public helper is compact and offset from the lead form; member and
  classroom surfaces keep helper access through in-nav actions instead of
  floating overlays.
- The OneTime frontend audit harness now recognizes real footers, scoped
  helper overlays, and the configured OneTime yellow state.

Local evidence:

- PASS `node --check scripts/audit-onetime-parallel-frontend.mjs`.
- PASS `node --check public/js/bna-bot-widget.js`.
- PASS `node --test tests/one-time-shared-review-branding.test.js
  tests/one-time-brand-helper-isolation.test.js`.
- PASS local static chrome audit at
  `ops/ui-audits/2026-07-10-onetime-static-chrome-local-server/` with 0 scoped
  public/member/classroom findings. The 34 remaining findings are only in the
  provider-review/Operations parity packet.

Deployment/live-smoke is still required before calling the static chrome UI
packet Done.

## UISTATE-20260710-002 - OneTime static chrome deployed

Commit `befe65c8` deployed to OneTime Railway deployment
`942b602c-150c-4587-a85f-07f7b80e21f3`, which reached `SUCCESS`.

Live evidence:

- OneTime separate-instance smoke passed on
  `https://join.onetimeonetime.com`.
- Rabbi OneTime landing smoke passed and confirmed focused OneTime branding and
  scoped Rabbi Scheller provider config.
- OneTime interest dry-run smoke passed without creating a real product lead,
  CRM lead, internal communication note, Telegram/email/WhatsApp send,
  checkout, access grant, Zoom meeting, or external write.
- Public route privacy smoke passed.
- Live visual audit at
  `ops/ui-audits/2026-07-10-onetime-static-chrome-live-readback/` shows status
  `captured`, 10 findings total, and 0 scoped static-route findings. All
  remaining findings are `/provider.html?review=one-time` provider parity
  scope.

The scoped static OneTime chrome lane is Done. Provider-review/Operations
parity remains separate. Full production remains blocked by external setup,
terminal Agent Mode proof, no unblocked execution batch, and Rabbi Telegram
hosted/live-smoke proof.

## UISTATE-20260710-003 - OneTime provider/Operations parity fixed locally

Codex implemented the remaining provider-review and scoped Operations visual
parity slice locally.

Local changes:

- Provider review and signed Rabbi provider session chrome now show a Provider
  active state and larger OneTime logo treatment.
- Scoped OneTime Operations routes now use larger OneTime brand marks,
  yellow/black active section tabs, compact top chrome, 44px mobile
  buttons/selects/inputs, and non-clipped section labels.
- The OneTime frontend audit harness no longer misclassifies the provider
  workspace sidebar as top chrome.

Local evidence:

- PASS focused provider/static tests with 25/25 passing.
- PASS PQC validation for
  `04-provider-operations-layout-parity-audit.product-quality.json`.
- PASS `npm run watchdog:protocol-drift`.
- PASS local visual audit at
  `ops/ui-audits/2026-07-10-onetime-provider-parity-local/` with status
  `captured`, 0 findings total, 0 provider findings, and 0 Operations findings.

Deployment/live-smoke is still required before this UI parity slice can be
called Done.

## UISTATE-20260710-004 - OneTime provider/Operations parity deployed

Commit `acd35fa7` deployed to OneTime Railway deployment
`bdae36da-b7e1-439b-ad1a-26eee321d2bf`, which reached `SUCCESS`.

Live evidence:

- OneTime separate-instance smoke passed on
  `https://join.onetimeonetime.com`.
- Rabbi OneTime landing smoke passed and confirmed focused OneTime branding and
  scoped Rabbi Scheller provider config.
- OneTime interest dry-run smoke passed without external writes.
- Public route privacy smoke passed.
- Authenticated live visual audit at
  `ops/ui-audits/2026-07-10-onetime-provider-parity-live-readback/` shows
  Operations auth `railway`, 0 skipped checks, status `captured`, 0 findings
  total, 0 provider findings, and 0 Operations findings.

The provider-review/Operations UI parity slice is Done. Full production remains
blocked by external setup, Agent Mode terminal proof, no unblocked execution
batch, and Rabbi Telegram hosted/live-smoke proof.

## RAW-20260710-001 - One Time ramble-to-terminal UI gap audit

Shloimie supplied the full ramble-to-terminal UI gap audit prompt from Downloads. Codex registered it as `RAW-20260710-001`, created the canonical source/lifecycle matrix, and created `ops/ui-audits/2026-07-10-onetime-ui-gap-register/report.md`. Manual review of the latest deployed screenshots found concrete current UI gaps despite the automated zero-finding audit: mobile nav clipping at 390px, OneTimeOneTime display-copy inconsistency, and provider long-text containment. Lag/performance is separately terminal for the measured 2026-07-09 issue because the latest live lag readback shows 0/18 slow samples after deployed fixes.
