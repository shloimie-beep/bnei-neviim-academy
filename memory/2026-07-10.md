# 2026-07-10

## LAGSTATE-20260710-001 - OneTime cache/static delivery lane opened

The earlier OneTime app-lag finding remains valid: live evidence showed slow
and variable first-byte timing across HTML, static JS/CSS, and small API
routes, while visual polish defects are tracked separately.

Current correction: the active UI/API collision lane has cleared. A fresh
control-tower readback on 2026-07-10 showed no dirty worktree at sample time,
no ready ChatGPT packets, no claimable agent-fleet jobs, and no active UI
collision lane.

Codex implemented the first conservative cache/static-delivery fix locally:
public JS/CSS assets get a short public cache, public media/fonts get a longer
public cache, and HTML, service worker, manifests, API/private routes, and
Operations shell assets remain no-store/no-cache.

Evidence:

- `server.js`
- `tests/bna-helper-tools.test.js`
- `tests/one-time-intake-api-readback.test.js`
- `tasks-pending/2026-07-09-onetime-parallel-frontend-audit.md`
- `tasks-pending/2026-07-09-production-readiness-goal.md`

Deployment update: commit `4908c905` was pushed and deployed to the OneTime
Railway target `one-time-production / one-time-web` as deployment
`50533728-970f-4936-bc14-bbe439992f6e`, which reached `SUCCESS`.

Live readback:

- `header-readback.md` proves public JS/CSS now use
  `public, max-age=300, must-revalidate`.
- OneTime logo media uses
  `public, max-age=86400, stale-while-revalidate=604800`.
- `/one-time/`, `sw.js`, `manifest.json`, `operations-shell.js`, and
  `operations-deferred-renderers.js` remain no-store/no-cache.
- Fresh no-write lag audit improved from 18/18 samples needing attention to
  1/18. The only remaining threshold finding was parent-review desktop
  `slow_dom_content_loaded`.

Remaining: continue runtime/parent-review performance follow-up only if repeated
samples stay slow; visual polish remains in the separate OneTime frontend audit.

## DEPLOYSTATE-20260710-002 - Main BNA cache policy live

The same static cache policy is also live on `https://bneineviimacademy.org`.
BNA Railway doctor showed service `skillful-motivation` deployment
`b9cd16e1-ac80-4310-9c42-c87541a0343c` reached `SUCCESS`.

Live readback confirmed public JS/CSS short caching, public media longer
caching, and no-store/no-cache for HTML, service worker, manifests, and
Operations shell assets. `npm run app:smoke:public-privacy` passed and
`/api/health` returned OK with database connected.

## LAGSTATE-20260710-003 - OneTime parent-review residual fixed locally

The single remaining lag threshold after the cache deploy was the desktop
`parent-review` route. Codex found that `/parent.html?review=one-time` was
serving the full BNA parent portal shell, about 284 KB, before switching into
OneTime review mode.

Local fix: keep the same canonical route URL but intercept the review query
before static file serving and send a dedicated lightweight
`public/one-time-parent-review.html` shell. Normal BNA parent routes still use
`public/parent.html`.

Local evidence:

- PASS `node --check server.js`.
- PASS `node --test tests/one-time-review-only-server.test.js
  tests/one-time-shared-review-branding.test.js
  tests/one-time-brand-helper-isolation.test.js`.
- PASS local no-write lag audit at
  `ops/performance-audits/2026-07-10-onetime-parent-review-lightweight-local/`
  with 0/18 samples needing attention.
- Parent-review desktop local sample: 25ms DCL, 32ms FCP, 531ms network idle,
  4 requests, 147 DOM nodes, no blockers.

Deployment/live-smoke is still required before calling the residual lag fix
Done.

## LAGSTATE-20260710-004 - OneTime parent-review residual deployed

Commit `9350c43a` deployed to OneTime Railway deployment
`a447201e-f28a-4111-ab59-f6c65ee64e58`, which reached `SUCCESS`. The main BNA
service also serves the same lightweight route from deployment
`105fabb5-63f1-4f6d-9e5f-0221c90fae65`.

Live evidence:

- `https://join.onetimeonetime.com/parent.html?review=one-time` and
  `https://bneineviimacademy.org/parent.html?review=one-time` both return the
  lightweight shell with body length 16886 and `Cache-Control: no-store`.
- Both live route readbacks include OneTime parent review markers and exclude
  the old `data-parent-onboarding-form` and `BNA Parent Portal` title.
- No-write live lag audit passed with 0/18 samples needing attention.
- Parent-review desktop live sample: 829ms DCL, 488ms FCP, 1325ms network idle,
  4 requests, 147 DOM nodes, no blockers.
- Live smokes passed for OneTime separate instance, Rabbi landing, OneTime
  interest dry-run, and BNA public/privacy.

The lag/UI response-time lane is now clear for cache/static delivery and the
parent-review residual. Visual polish remains a separate UI packet, and full
production remains blocked by external setup, Agent Mode terminal proof, and
Rabbi Telegram hosted/live-smoke proof.
