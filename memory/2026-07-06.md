# 2026-07-06

## RAW-20260706-951 - ChatGPT Agent Dropoff Collector And Fleet Status

Raw source saved at:

- `raw-input/RAW-20260706-951-chatgpt-agent-dropoff-collector-and-fleet-status.md`

Parsed workflow update:

- One attached ChatGPT Agent Mode audit run failed because that ChatGPT session
  had no GitHub connector enabled and returned `CANNOT_WRITE_GITHUB`.
- The related One Time prompt-series marked GitHub comment was repaired,
  collected, and queued as live task `#1945` / agent job `#397`.
- The agent fleet queue sees the job, but the supervisor is not running, so the
  fleet is not actively working it yet.

Register:

- `tasks-pending/2026-07-06-chatgpt-agent-dropoff-collector-and-fleet-status.md`

## RAW-20260706-002 - One Time Studio Sidekick And OpenArt MCP Scope

Raw source saved at:

- `raw-input/RAW-20260706-002-onetime-studio-sidekick-openart-mcp.md`

Parsed product/system request:

- Build a narrow sidekick for the future One Time AI Studio operator.
- Scope it to `rabbi_sheller_provider / one_time_mishnah_class`.
- Let the operator use natural language to generate, store, inspect, and patch prompts.
- Let the operator upload AI render images and ask for prompt corrections such as realism, clothing/hat, character consistency, and scene quality.
- Use a hosted multimodal GPT-style model for image critique and prompt drafting, not Codex as the routine creative sidekick.
- Use OpenArt MCP only through an approved account/OAuth path and within scoped Studio actions.
- Do not give this operator broad website, admin, external-send, payment, secret, deployment, or arbitrary CLI power.

Register:

- `tasks-pending/2026-07-06-onetime-studio-sidekick-openart-mcp.md`

Status:

- Planning/register only.
- No code, assistant permissions, OpenArt OAuth/MCP connection, CLI access, image upload, external generation, or production mutation was performed.

## RAW-20260706-003 - One Time Studio Sidekick Goal Mode Build Approval

Raw source saved at:

- `raw-input/RAW-20260706-003-onetime-studio-sidekick-goal-mode-build.md`

Parsed goal-mode update:

- Shloimie approved goal-mode execution for the One Time Studio sidekick build.
- The future Studio operator should have a natural-language lane that can talk to a CLI/Codex-style repair system only for Studio layout and functionality.
- The operator must not be able to request arbitrary website, workspace, admin, secret, deployment, external-send, payment, or access changes through that lane.
- OpenArt signup/OAuth is deferred until after the BNA-side build is done; live MCP/OAuth remains a blocker, but mock/no-live adapter and UI/workflow can be built now.
- "OpenAI MCP" in the ramble is interpreted as the OpenArt MCP from the prior discussion unless corrected.

Register:

- `tasks-pending/2026-07-06-onetime-studio-sidekick-openart-mcp.md`

Status:

- Goal tool started.
- Implementation must begin with Product Quality Compiler / Definition of Ready before app-visible edits.
- No CLI access, assistant permission, OpenArt/OAuth connection, production mutation, deploy, external send, payment/access change, or code implementation was performed by this raw capture.

## One Time Studio Sidekick Local Build Closeout

Implemented locally for `RAW-20260706-002` / `RAW-20260706-003`:

- Added the env-backed `one_time_ai_studio_operator` role scoped to Studio and Tasks.
- Added a narrow route guard for that role: Studio APIs, Tasks, shared boot/read-only shell endpoints, and assistant scope-plan only.
- Added a no-live Studio sidekick flow for image/render observations, prompt corrections, reversible patch previews, OpenArt prompt export/copy, and Studio repair planning.
- Added an OpenArt MCP adapter boundary that returns readiness, prompt export/request plans, and a clear OAuth blocker without making live OpenArt calls or spending credits.
- Added a mediated Studio-only repair lane with file/route/test allowlists and explicit no-shell/no-raw-CLI/no-deploy/no-secrets/no-external-write constraints.
- Added route/action registry rows, product-quality packet, surface map, durable memory topic notes, unit/static tests, and browser smoke evidence.

Verification:

- `node --check server.js`
- `node --test tests/assistant-scope-policy.test.js tests/one-time-studio-sidekick-policy.test.js tests/one-time-studio-openart-adapter.test.js tests/one-time-studio-operator-role.test.js tests/service-provider-studio-domain.test.js tests/service-provider-studio-api-contract.test.js tests/service-provider-studio-operations-ui.test.js`
- `BNA_SERVICE_PROVIDER_STUDIO_SMOKE_DIR=ops/playwright-smokes/2026-07-06-one-time-studio-sidekick-local node --test tests/service-provider-studio-browser-smoke.test.js`
- `npm run pqc:validate -- ops/prompt-packets/2026-07-06-onetime-studio-sidekick-openart-mcp/00-control-tower.product-quality.json`
- `npm run watchdog:actions`
- `npm run watchdog:protocol-drift`

Remaining blockers:

- Live OpenArt OAuth/MCP account connection is blocked until Shloimie signs up and connects the account/workspace.
- True uploaded-image pixel analysis is blocked until the hosted multimodal model/provider, retention/privacy policy, and credentials are approved.
- Production deploy/live smoke still needs publish/deploy closeout.

## RAW-20260706-001 - BNA Parent Meeting Reminder Send And Merge

Raw source saved at:

- `raw-input/RAW-20260706-001-bna-parent-meeting-reminder-send-and-merge.md`

Parsed operations request:

- Send the BNA parent meeting-location reminder today by WhatsApp and email.
- Use Hebrew copy for Hebrew-tagged parents and verify it does not come out as
  mojibake/repeated question marks.
- Meeting location: 8 Havakuk Hanavi, Ramat Beit Shemesh Gimel.
- Tell parents not to go to the Webers today. Later correction: the Webers are
  just away/on vacation and are not hosting today's meeting; do not describe
  this as a permanent hosting change.
- Restrict the send to parents tied to current BNA student records.
- Merge duplicate Huda/Hooda Weber records.
- Merge duplicate Menachem records.
- Store the durable correction that Shloimie's clear natural-language approval
  is sufficient for an exact obvious send; do not require a typed magic phrase
  on top of that.

Register:

- `tasks-pending/2026-07-06-bna-parent-meeting-reminder-send-and-merge.md`

Status:

- Natural-language approval correction recorded in `AGENTS.md`, `MEMORY.md`,
  and `memory-topics/communications-intake.md`.
- Huda/Hooda Weber duplicate source `#21982` repaired to inactive/merged under
  target `#82261`; signup `#9` now points to `#82261`.
- Menachem duplicate source `#79458` merged into target `#2800`; signup `#12`
  now points to `#2800`.
- Email reminder sent to 5 individual parent email recipients: 3 Hebrew, 2
  English.
- WhatsApp first attempt was blocked by WAPI/Whapi provider authorization, then
  Shloimie approved retrying. Retry sent 8 WhatsApp reminders: 4 Hebrew and 4
  English. WAPI readback for communications `#2397` through `#2404` shows
  sent/delivered/read outcomes with no follow-up required.
- After Shloimie corrected the Weber wording, Codex sent a correction addendum
  by WhatsApp to 8 recipients: 4 Hebrew and 4 English, and by Gmail to 5
  individual parent email recipients: 3 Hebrew and 2 English. The addendum says
  the Webers are just away/on vacation and are not hosting today's meeting.

## RAW-20260706-901 - Deploy Gate Scoped Credential Deferral

Raw operator correction:

> I thought you fixed that issue, that you're able to still push stuff and deploy stuff even though we're missing those keys. And what does Vimeo or Stripe or Telegram have to do anything? Why can't I deploy stuff if I'm missing those keys? I already told you that before.

Parsed requirement:

- `REQ-20260706-901`: Missing unrelated provider credentials/readback must not block scoped app/UI deploys after explicit deploy approval. They should remain deferred readiness findings unless the scoped work actually depends on that integration or claims live verification/final closeout for it.

## RAW-20260706-902 - OpenArt MCP Endpoint

Raw operator input:

> https://mcp.openart.ai/mcp

Parsed requirement:

- `REQ-20260706-902`: Use `https://mcp.openart.ai/mcp` as the canonical OpenArt MCP endpoint for the One Time AI Studio integration scope. Direct endpoint readback returned `401 Unauthorized`, so live Studio integration still needs OpenArt auth/token setup before API/MCP calls can be verified.

## RAW-20260706-903 - ChatGPT Dropoff Status And One Time Audit Setup

Raw source saved at:

- `raw-input/RAW-20260706-903-chatgpt-dropoff-status-and-onetime-audit-setup.md`

Parsed operations request:

- Audit whether the no-paste ChatGPT-to-Codex dropoff workflow is real,
  documented, tested, and connected to the agent fleet.
- Clarify repo-file packet mode, GitHub-comment fallback, agent pickup status,
  and credential/password prompt policy before creating broad Agent Mode audit
  prompts.
- Prepare the next prompt series around the canonical One Time Mishnah target:
  `https://join.onetimeonetime.com/`.

Register:

- `tasks-pending/2026-07-06-chatgpt-dropoff-status-and-onetime-audit-setup.md`

Status:

- Dropoff repo-file packet mode exists and is documented.
- GitHub-comment fallback exists and targeted marked-comment collection works.
- Ingestor exists and terminal smoke packet readback was skipped as already
  `done_verified`.
- Agent fleet hook is enabled, but the supervisor was not running in this
  local readback.
- Focused dropoff/collector/fleet tests passed 15/15.
- Broad GitHub comment scan hit a TLS handshake timeout once; targeted comment
  URLs are safer for the next prompt packet.
- Real passwords/secrets must not be embedded in prompts.
- Next One Time audit prompt series remains pending.

## RAW-20260706-904 - Finish Pending Work Inventory

Raw source saved at:

- `raw-input/RAW-20260706-904-finish-pending-work-inventory.md`

Parsed operations request:

- Inventory what is pending, not pushed, and actively being worked on.
- Finish safe repo-visible closeout work through verification, commit, push,
  PR/merge, and deployment/live smoke where the guarded release path allows it.
- Leave exact blockers for work that depends on credentials, external account
  setup, production approval, or human decisions.

Register:

- `tasks-pending/2026-07-06-finish-pending-work-inventory.md`

Initial status:

- Current checkout was clean and pushed, but branch
  `codex/bna-parent-reminder-send-20260706` had no PR and had drifted from
  `origin/master` by two commits each way.
- Active run
  `ops/execution-runs/2026-07-02-background-drive-ui-launch-continuation`
  validates and has no next unblocked executable batch.
- PR #64 is already merged, while the active run still contains stale PR #64
  closeout language.
- PR #105 is the only open PR and GitHub reports it with dirty merge state.

Closeout status:

- Parent reminder closeout was published through PR #108 and merged to master
  at `70001fcc0beb595c18e75098c501289500d8289c`.
- One Time Studio sidekick PR #105 was updated against master, verified,
  pushed, merged to master at `8f2c95958084e05f379c23fe9b68d4e09c4994e0`,
  deployed to Railway service `skillful-motivation` as deployment
  `42d7cfa8-7a39-4371-b799-46a62d88aadc`, and live-smoked successfully.
- Active run
  `ops/execution-runs/2026-07-02-background-drive-ui-launch-continuation`
  still has 6 done and 4 blocked requirements, with no next unblocked batch.
- Remaining blockers are external/account decisions: OpenArt OAuth/MCP account,
  true uploaded-image pixel analysis model/provider/budget/privacy policy,
  remaining provider aliases/credentials, and active-run DB/bootstrap items.

ChatGPT Agent Mode dropoff smoke prompt:

- Added
  `ops/prompt-packets/2026-07-06-chatgpt-dropoff-smoke-agent-mode/00-smoke-prompt.md`
  as a no-op prompt to test whether a ChatGPT Agent Mode session can create a
  repo-visible packet or marked GitHub comment without manual copy/paste.
  It forbids secrets, app/source edits, sends, payments, access grants, DNS,
  provider mutations, Drive writes, production data changes, and deploys.

## RAW-20260706-907 - One Time Studio Worker Handoff Continuation

Raw source saved at:

- `raw-input/RAW-20260706-907-onetime-studio-worker-handoff-continuation.md`

Parsed operations request:

- Continue `RAW-20260702-010` / `TASK-20260702-010` from the merged Studio
  packets and code.
- Do not redo finished slices.
- Implement the next unblocked local batch so source intake can flow to
  storyboard, scene prompt pack, and AI video worker review handoff.
- Leave live OpenArt/OAuth/MCP, generation/upload/credit spend, and hosted
  multimodal image-analysis model/privacy decisions as exact blockers.

Initial status:

- Local implementation added an idempotent `ai_video_worker_handoff` Studio
  export route, a worker prompt-pack builder, Operations UI card/action, route
  and action registry rows, and focused tests.
- No OpenArt call, upload, generation, credit spend, external send, payment,
  access grant, DNS change, credential change, provider-account mutation,
  Drive write, or production data mutation was performed.

## RAW-20260706-908 - One Time AI Video Worker Access Role

Raw source saved at:

- `raw-input/RAW-20260706-908-onetime-ai-video-worker-access-role.md`

Parsed operations request:

- Add a new scoped `one_time_ai_video_worker` role.
- The role may access only One Time Studio and the One Time task manager for
  `rabbi_sheller_provider` / `one_time_mishnah_class`.
- The role must not see broader Rabbi/provider admin data, BNA data, contacts,
  payments, settings, integrations, agent fleet, queue health, task artifacts,
  raw shell/Codex/deploy/secrets, external sends/publishes/uploads, or Content
  handoff.
- Closeout requires tests, route/action registry updates, and a clear access
  matrix.

Register:

- `tasks-pending/2026-07-06-onetime-ai-video-worker-access-role.md`

Initial status:

- Implementation started against the existing One Time role model, task-manager
  scoping, and Studio handoff packet work.

Closeout:

- PR #112 merged to `master` at
  `925c54fde1fd98cd662e4bd8aa222a0997e08fb5`.
- Railway production deployment
  `dfb8487b-7c27-483b-95f5-afffc4a4d26e` reached `SUCCESS`.
- Live Studio worker-handoff smoke passed:
  `ops/live-smokes/2026-07-06T13-02-19-489Z-studio-ai-video-worker-handoff-live-smoke.md`.
- The smoke created/reused TEST Studio project `3`, saved source `1`,
  generated 2 storyboard scenes, compiled 9 prompt layers, built a worker prompt
  pack with 2 scene prompts, and created AI video worker handoff export `1`.
- A stale live DB constraint
  `bna_studio_prompt_layers_type_check` was repaired to include
  `jewish_guardrails`, matching the repo migration.
- Remaining exact blockers: production worker login needs
  `ONE_TIME_AI_VIDEO_WORKER_USERNAME` / `ONE_TIME_AI_VIDEO_WORKER_PASSWORD`;
  OpenArt live generation/upload/credit spend needs account/OAuth/API
  credentials and approved model/cost/privacy policy; true uploaded-image pixel
  analysis needs an approved hosted multimodal provider/model, budget,
  retention/privacy, and image-upload policy.

## RAW-20260706-950 - One Time Operations Dashboard UI Hotfix

Raw source saved at:

- `raw-input/RAW-20260706-950-onetime-operations-dashboard-ui-hotfix.md`

Parsed requirement:

- The exact live Operations URL
  `/operations?view=dashboard&section=overview&workspace=rabbi_sheller_provider`
  must render a scoped One Time provider overview, not the generic BNA
  Operations dashboard.
- The scoped Rabbi/One Time sidebar should not expose the full workspace
  directory categories.
- Studio must be visible as a One Time module.
- One Time nav/topbar labels should be full and consistent.

Initial status:

- Local hotfix started in a clean worktree on
  `codex/onetime-dashboard-ui-fix-20260706`.

Closeout:

- PR #118 merged to `master` at
  `f0b7cc0a80f35a79cbcd843844fe90960152938b`.
- Railway production auto-deploy
  `96961b6f-1ae6-467d-89ae-2a9a5c088753` reached `SUCCESS`.
- Exact authenticated live browser smoke passed for the reported URL:
  `ops/live-smokes/2026-07-06T14-10-22-946Z-one-time-operations-dashboard-ui-live-smoke.md`.
- Remaining separate blocker: product-quality protocol drift still reports
  pre-existing findings in the One Time full UI agent-audit prompt packet lane.

## RAW-20260706-956 - One Time Operations Content IA / Section Rail

Raw source saved at:

- `raw-input/RAW-20260706-956-onetime-operations-content-ia-filter-rail.md`

Parsed requirements:

- One Time Rabbi Operations needs a visible `Classes & Content` sidebar category.
- The One Time content top rail should show compact, non-repetitive sections:
  `Library`, `Meeting Drops`, `Source Prep`, and `Bundles`.
- Mobile top section rail should slide horizontally without document overflow or
  extra meta-row height.
- Provider Program should keep short top sections and provide a bridge to the
  real Classes & Content workspace.

Local status:

- Implemented and locally verified in clean worktree branch
  `codex/onetime-ia-filters-content-20260706`.
- Pending closeout: commit, push, deploy, and live smoke.

## RAW-20260706-909 - One Time CRM Mailbox Goal

Raw source saved at:

- `raw-input/RAW-20260706-909-onetime-crm-mailbox-goal.md`

Register:

- `tasks-pending/2026-07-06-onetime-crm-mailbox-goal.md`

Parsed operations request:

- Build a real Rabbi / One Time CRM mailbox for `info@onetimeonetime.com` so
  Rabbi can log in and see received emails.
- Use the existing Resend inbound/sender path where possible.
- Add a polished provider-portal mailbox UI with CRM thread structure,
  readiness, local draft, and guarded reply send.
- Keep bulk email/campaign sending as a separate guarded packet.
- Store the operator-provided physical mailing address only in runtime
  config/keyholder, not tracked source.

Publication-branch status:

- Created clean branch `codex/onetime-crm-mailbox-20260706` from current
  `origin/master` and applied only the scoped mailbox patch.
- Pushed the clean branch and opened draft PR #117:
  `https://github.com/shloimie-beep/bnei-neviim-academy/pull/117`.
- Added provider-session gated One Time mailbox APIs reading
  `bna_communications` email threads scoped to `rabbi_sheller_provider` /
  `one_time_mishnah_class`.
- Added provider portal Mailbox section with search, thread list, email
  timeline, readiness chips, draft save, and guarded send controls.
- Added route/action registry rows and focused contract tests.
- Verification passed locally on the clean branch: `node --check server.js`;
  inline provider script parse; `node --test tests/provider-mailbox-portal.test.js`;
  combined provider mailbox/WAPI tests; PQC validation; and `npm run
  watchdog:actions`.
- Follow-up release-gate work fixed the protocol-drift false positive against
  the One Time full-UI audit manifest and added required audit protocol markers;
  `npm run watchdog:protocol-drift` now passes.
- Redacted One Time Railway readback confirms `RESEND_WEBHOOK_SECRET`,
  `RESEND_FROM_NAME`, `RESEND_REPLY_TO`, and `ONE_TIME_MAILING_ADDRESS` are
  configured on `one-time-web` production. The exact physical address remains
  runtime-only and is not tracked.
- No-send integration smoke passed for Resend with API/domain/sender readiness
  and `send_allowed=true`; no email send was performed.
- PR #117 was merged to `master` at
  `61d1da3e3ea683358685350dd2f9af79ba39f478`; `one-time-web` production
  deployment `733aee09-bd85-4b97-9256-0d7ba5837a08` reached `SUCCESS`.
- Live One Time instance smoke passed for
  `https://join.onetimeonetime.com`.
- Created the Rabbi provider mailbox login on provider id `1` with username
  `one_time_admin`; the generated password is stored only in
  `C:/Users/User/BNA v2.0/.secrets/one-time-provider-mailbox-login-20260706.txt`.
- Authenticated provider mailbox readback passed: 4 live threads for
  `info@onetimeonetime.com`; first thread detail opened with one message.
- Live readiness confirmed sender/reply-to present and mailing-address
  configured without committing the physical address.
- Evidence:
  `ops/live-smokes/2026-07-06T14-49-16-448Z-one-time-provider-mailbox-live-readback.md`.
- No external email send, bulk campaign, DNS change, payment change, fake
  inbound insert, password print, message-body evidence capture, or physical
  address commit was performed.

### One Time Mailbox Old Email Backfill

- Follow-up operator question: what happened to the old emails from the past
  couple of days.
- Live mailbox before backfill had 4 threads, all outbound rows from
  2026-07-02, with 0 inbound Resend-received rows.
- Resend Receiving API still had 9 received emails for
  `info@onetimeonetime.com`, dated 2026-06-29 through 2026-06-30.
- Backfilled those 9 actual Resend received-email ids through the deployed
  signed `/api/resend/inbound` handler; communication ids 17-25 were created.
- Live mailbox after backfill: 12 threads, 9 inbound messages, 4 outbound
  messages, and 1 thread with attachment metadata.
- Evidence:
  `ops/live-smokes/2026-07-06T15-03-36-636Z-one-time-mailbox-resend-backfill.md`.
- No email was sent; no raw email bodies, sender addresses, received-email ids,
  subjects, password, or physical address were printed or committed.

### One Time Local Class Welcome Send

- Operator provided a private Zoom Mishnayos invite link and three Gmail
  addresses for local class attendees, asking Codex to add them to the One
  Time CRM and send a simple welcome/feedback email.
- Repo evidence redacts the full Zoom URL and recipient addresses because they
  are private contact/access data.
- Codex fixed two live backend blockers before sending: generic Resend draft
  sends now preserve One Time mailbox scope metadata, and the cross-workspace
  recipient-conflict check now maps `bna_workspace_settings.workspace_key`
  instead of querying a nonexistent `project_key` column.
- one-time-web deployments `05f259e2-19fd-4efd-a385-955c3e3f4a72` and
  `edea316e-1831-4679-af1e-c861714839d4` reached `SUCCESS`; One Time live
  smoke passed.
- Final live operation upserted three scoped local-class CRM leads, sent three
  individual welcome emails through `SEND_RESEND_EMAIL`, logged CRM notes, and
  provider mailbox readback found three matching welcome threads.
- Evidence:
  `ops/live-smokes/2026-07-06T15-23-44-976Z-one-time-local-class-welcome-send.md`.
  No shared-recipient/bulk campaign, WhatsApp, payment/access, DNS,
  member/library entitlement, or external CRM write was performed.

## One Time Vimeo / Stripe Scope And Folder-Library Workflow

- Registered renumbered raw records `RAW-20260706-965` through
  `RAW-20260706-969` for the Vimeo/Stripe automation status, One Time scoping
  correction, Vimeo folder-library workflow, safe-command preference, and Stripe
  sandbox webhook handoff. Older local-only ids conflicted with records already
  on `master`, so the clean publish branch uses the non-colliding ids.
- Built a no-write-by-default One Time Vimeo folder-to-library CLI:
  `npm run one-time:vimeo-library`. It scans
  `media-inbox/one-time-vimeo-drop`, creates redacted review reports, and
  requires exact environment/flag gates before any real Vimeo upload, DB review
  package write, or member-library publish.
- Vimeo and Stripe work for One Time remains scoped to
  `rabbi_sheller_provider` / `one_time_mishnah_class`; BNA Academy media or
  payment records must not be reused as One Time records without an explicit
  cross-workspace link.
- Stripe sandbox handoff/report exists, but live/test payment work still needs
  exact Stripe keys, webhook secret, price/product ids, and explicit approval
  before any charge, payment link, refund, or access grant.

## Clean Deploy All Pending Work

- Registered `RAW-20260706-970` and requirement register
  `tasks-pending/2026-07-06-clean-deploy-all-pending.md` for the broad cleanup
  request to inventory undeployed ramble-derived work, debug safe changes, ship
  only eligible scoped work, and explain next steps plainly.
- Inventory finding: the original checkout was dirty and not a safe deploy
  source, so Codex created a clean worktree branch
  `codex/clean-deploy-all-pending-20260706` from current `origin/master` and
  copied only the intended safe Vimeo/Stripe/cleanup records and workflow files.
- Live readbacks before this clean publish showed current `master`
  `4466737a` deployed successfully on BNA Railway production, One Time join
  health/db checks passing, app smokes passing, no open GitHub PRs, and the
  active execution run at 8 done / 2 blocked with no unblocked executable batch.

## Local Checkout Archive And Clean Reset

- Registered `RAW-20260706-980` for the operator request to archive, clean,
  delete stale local leftovers, publish, and push the cleanup closeout.
- Archived the dirty `C:\Users\User\BNA v2.0` checkout before destructive
  cleanup to local-only archive:
  `C:\Users\User\BNA-local-archives\2026-07-06-bna-v2-dirty-checkout-before-reset`.
- The archive captured status files, patches against local HEAD and
  `origin/master`, current copies of 34 tracked modified files, and 38
  untracked files. The archive is outside the repo and intentionally not
  committed.
- Reset the local checkout to `origin/master`, removed untracked stale files
  with `git clean -fd`, switched to clean branch
  `codex/archive-clean-checkout-closeout-20260706`, and removed the stale local
  `codex/onetime-studio-worker-handoff-20260706` branch after it had already
  been merged into `master`.
- Published and merged cleanup PR #124. Railway production deployment
  `51f37ce6-6a23-4622-abc2-c474184f1f4f` reached `SUCCESS` on commit
  `9beee40ae2c6f82cd6f6fea15661b4ac082f1543`.
- Archived 93 stale registered Git worktrees to
  `C:\Users\User\BNA-local-archives\2026-07-06-bna-stale-worktrees-before-remove`,
  then removed the stale worktree folders and deleted 139 old local branches
  after creating an all-local-refs recovery bundle. Final local state is one
  registered worktree and one clean local branch: `master`.
- Verification passed: tracked secrets audit, BNA live app smoke, One Time
  landing smoke, One Time separate-instance smoke, BNA health, One Time health,
  no open GitHub PRs, and clean local `master...origin/master` status.
- Guardrails: no archive contents, keyholder diagnostics, old superseded raw
  IDs, external sends, payments/access, DNS/provider changes, Drive writes, or
  secret values were committed.
