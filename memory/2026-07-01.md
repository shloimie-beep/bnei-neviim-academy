# 2026-07-01 Memory

## RAW-20260701-001 - Ramble Protocol v3 Product Quality Operating System

- Source: Codex chat `BNA_GOAL_MODE_EXECUTION_PACKET`.
- Scope: protocol/tooling enforcement only.
- Requirement register:
  `tasks-pending/2026-07-01-ramble-protocol-v3-product-quality-operating-system.md`.
- Added Ramble Protocol v3 so vague product-quality rambles route through:
  raw capture, Ramble Router classification, Product Quality Compiler
  expansion, Super-Ramble Packet DAG, control tower, current-state visual
  audit, Definition of Ready, small implementation packets, verification,
  deploy/live-smoke gate where app-visible, trace, drift watchdog, and
  closeout.
- Added or updated docs for the Ramble Router, Product Quality Operating
  System, Packet DAG, context budget / packet splitting, repo surface maps, UI
  pattern reference, visual harness, browser-agent security, and agent trace
  observability.
- Extended Product Quality Compiler schema/validator/watchdog/evals for v3
  router/DAG/context/audit-first requirements and prompt-injection handling.
- Added packet templates for control tower, current-state visual audit,
  implementation, verifier closeout, and provider setup.
- Added Rabbi Sheller / One Time sample packets showing the next flow starts
  with `00-control-tower` and `01-current-state-visual-audit`, not UI
  implementation.
- Verification passed: `npm run pqc:all`, Product Quality Compiler fixtures,
  evals 8/8, invalid fixture checks 7/7, protocol drift watchdog with 0
  findings, trace validator, BNA run validators, source coverage, stale
  evidence, secrets audit, JSON/JSONL validation, and `git diff --check`.
- Guardrails held: no Rabbi UI implementation, no email, no Stripe, no DNS or
  provider mutation, no payment/access grant, no Drive/Zoom/Vimeo/WhatsApp/
  Telegram external write, no external CRM write, and no GHL/LeadConnector
  runtime.
- Deployment/live smoke: not required for this packet because only docs,
  templates, fixtures, and local validation tooling changed; no app-visible or
  server-visible runtime behavior was changed.
- Next exact packet: Generate Rabbi Sheller / One Time `00-control-tower` and
  `01-current-state-visual-audit` packets using Ramble Protocol v3 / Product
  Quality Operating System.

## RAW-20260701-002 - Huda Progress Correction

- Source: Codex chat operator correction.
- Requirement: `REQ-20260701-013` set only Huda Weber's cumulative Torah/trip
  progress to 34% and recalculate the group percentage.
- Production DB readback after transaction: Huda 34%, Hillel 34%, Menachem
  Mendel 27%, Eitan Chaim 32%, Amitai 38%, overall 33%.
- Guardrails held: no other student percentage directly changed, no raw
  transcript/private message body recorded, and no sends, payments, access
  grants, DNS, credentials, or external provider writes.

## RAW-20260701-003 - Finish Product Quality Protocol, Brand Kits, Rabbi Control Tower

- Source: Codex chat `BNA_GOAL_MODE_EXECUTION_PACKET`.
- Requirement register:
  `tasks-pending/2026-07-01-finish-product-quality-protocol-brand-kits-rabbi-control-tower.md`.
- Corrected brand source of truth:
  - Rabbi / One Time brand = black + yellow.
  - BNA brand = cream + navy + teal/cyan.
  - Config sources: `config/brands/one-time.json` and
    `config/brands/bna.json`.
- Corrected pipeline source of truth:
  - Rabbi / One Time uses shared first-party BNA platform primitives and
    operating standards, but has a separate provider-specific classroom/
    content/community pipeline scoped to `rabbi_sheller_provider` /
    `one_time_mishnah_class`.
  - BNA Academy, Rabbi / One Time, and future providers do not share classroom/
    content/community records without explicit cross-workspace links.
- Added memory-topic lookup and contradiction handling for brand, design,
  pipeline, provider, email, Stripe, CRM, community, WhatsApp, screenshots, and
  `I already told you` corrections.
- Added design-reference capture protocol at `docs/DESIGN-REFERENCE-CAPTURE.md`
  and a brand correction package under
  `ops/design-references/2026-07-01-brand-kit-correction/`.
- Created provider setup packets:
  - `ops/prompt-packets/2026-07-01-provider-config-readback/09-resend-send-enabled-smoke.md`
  - `ops/prompt-packets/2026-07-01-provider-config-readback/10-stripe-sandbox-smoke.md`
- Created Rabbi / One Time first real UI workflow packets:
  - `ops/prompt-packets/2026-07-01-rabbi-onetime-ui-cleanup/00-control-tower.md`
  - `ops/prompt-packets/2026-07-01-rabbi-onetime-ui-cleanup/01-current-state-visual-audit.md`
- Validation passed in the dirty main workspace and a clean release worktree:
  `npm run pqc:all`, BNA run status/validate/source-coverage/stale-evidence,
  secrets audit, focused One Time/brand tests, JSON parsing, trace validation,
  protocol drift watchdog, and `git diff --check`.
- Guardrails held: no Rabbi UI implementation, no screen redesign, no GHL,
  no DNS mutation, no live Stripe, no bulk campaign, no email send, no hard
  delete, and no secrets exposed.
- Commit/PR status is tracked in the requirement register; the safe release path
  uses clean worktree `C:\Users\User\BNA-product-quality-protocol-release-20260701`
  because the main workspace has unrelated dirty changes.
- PR #59 merged to `master`:
  `https://github.com/shloimie-beep/bnei-neviim-academy/pull/59`.
- Merge commit: `1fd7ddcc514cc7e0e3f98b7787c7b177d38f376a`.
- Railway deployment/live smoke was blocked by `npm run railway:target:doctor`
  because no explicit Railway project and service target is configured; no
  deploy/live-smoke proof was claimed.
## RAW-20260701-004 - Namecheap .com renewal risk audit

Raw operator wording:

> Figure out whether the repeated Namecheap auto-renew failures for `bneineviimacademy.com` actually endanger anything in this project or if they can be safely ignored. Trace every live dependency on `.com` versus `bneineviimacademy.org`, Railway, Replit, and One Time email/domain setup, then give me an exact renew-or-migrate recommendation with the specific repo evidence behind it. Yeah, I just clicked on this little thing in the bottom. It's in my email. Um, aren't we going through Google or something? What do I have to do over here?

Parsed items:

- REQ-20260701-004: Audit live repo/config/DNS dependencies for `bneineviimacademy.com` versus `bneineviimacademy.org`, Railway, Replit, and One Time email/domain setup.
- DEC-20260701-004: Operator needs an exact renew-or-migrate recommendation for the failed Namecheap auto-renew notice, including whether any immediate account action is required.

Follow-up:

> Yeah, I realized that the website wasn't working. So, um, the .com, I want the .com to forward to the .org. I don't know if that's actually happening now. But, um, I think I just had to pay for that .org.

> How long does it take till it starts working again? Because right now it's not working.

Audit summary:

- `https://bneineviimacademy.org/` loaded successfully from external Codex web tooling.
- Public resolvers returned `.org` A and Google MX records; `.com` and `www.com` point to Namecheap forwarding IPs.
- `http://bneineviimacademy.com` and `http://www.bneineviimacademy.com` return a Namecheap `301` redirect to `https://bneineviimacademy.org`.
- HTTPS on `.com` is not reliable in local checks; use an SSL-capable redirect path if `https://bneineviimacademy.com` needs to work.
- Audit artifact: `ops/audits/2026-07-01-domain-renewal-forwarding-audit.md`.

## RAW-20260701-005 - One Time Resend Secret And Send Readiness

- Source: Codex chat `BNA_GOAL_MODE_EXECUTION_PACKET`.
- Requirement register:
  `tasks-pending/2026-07-01-onetime-resend-secret-send-readiness.md`.
- Keyholder path supplied by operator: `C:\Users\User\BNA-Keyholder`.
- Resend API key and webhook secret are present in keyholder and Railway by
  redacted fingerprint only.
- Added loader-compatible keyholder alias for `RESEND_WEBHOOK_SECRET` and
  One Time Resend config files for sender/domain/reply-to/account owner.
- Railway `skillful-motivation` production variables were set and deployed;
  final Resend code patch deployment is
  `99b21d37-1297-40c4-841e-8dca32ddf8d5` from clean `origin/master` commit
  `57af99434f83fb2af5af45946d15ac019e13953f`.
- Live status after deployment: Resend configured, webhook configured,
  `required_env=[]`, domain verified, and `send_allowed=true`.
- Resend webhook is enabled at `https://bneineviimacademy.org/api/resend/inbound`
  with selected and recommended events, including `email.received`,
  `email.sent`, `email.delivered`, `email.clicked`, `email.bounced`,
  `email.opened`, `email.failed`, `email.suppressed`, `email.complained`,
  and `email.delivery_delayed`.
- Safe test sends were performed only to official Resend test addresses and
  one owned inbox address. No imported lead/contact or campaign segment was
  sent.
- Official test-address webhook readback processed `email.sent`,
  `email.delivered`, `email.bounced`, and `email.suppressed`; invalid Svix
  signatures return `401`.
- Remaining blocker: real `email.received` fetch/CRM proof needs an external
  sender or actual Resend replay. Gmail connector requires reauthentication.
- Campaign decision: real/bulk campaign send is not authorized until final
  copy and exact final recipient list/segment are explicit.
