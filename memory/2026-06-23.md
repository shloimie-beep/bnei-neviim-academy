# 2026-06-23

## RAW-20260623-001 - Drive class intake parser investigation

Raw operator request:

> I need you to find out what's happening with my drive, my intake. There's a whole bunch of classes that I uploaded, and the score for the kids isn't getting updated. The kids' questions aren't getting updated. It's not getting parsed and updating the classes with the kids' profiles. Can you figure out what's going on? What happened to all these classes? Why they're not getting parsed and put into the system?

Distilled result:

- Recent Drive class files are visible and have live content jobs, so Drive pickup is not the main failure.
- Jobs 75-79 are blocked or stalled at transcription because Railway's live OpenAI transcription key returns HTTP 401 `invalid_api_key`.
- Local OpenAI diagnostics pass through the keyholder key, so the likely repair is to rotate/update Railway web and worker envs, then deploy and reprocess.
- Older parsed jobs exposed a parser-path regression: class notes and review items could be filed while score/progress arrays stayed empty. `server.js` was locally patched to run the scoring-aware progress parser alongside canonical intake filing.
- Production deployment, Railway key rotation, and job reprocessing remain blocked until explicitly authorized.

Tracking:

- Raw input: `raw-input/RAW-20260623-001-drive-class-intake-parser-stuck.md`
- Register: `tasks-pending/2026-06-23-drive-class-intake-parser-stuck.md`
- Evidence: `ops/drive-audits/2026-06-23T06-29-03-146Z-google-drive-audit.md`, `ops/drive-audits/2026-06-23T06-33-live-content-jobs-summary.md`, `ops/qa-runs/2026-06-23T06-32-54-117Z-openai-diagnostics.md`, `ops/watchdog-audits/2026-06-23T06-35-content-routing.md`

Follow-up after operator approval:

- Railway web and worker OpenAI keys were updated from the working keyholder source without printing values.
- Web deployment `88b20edb-272a-4880-a5da-c622564b74ab` reached `SUCCESS` after an initial bad reduced bundle was corrected.
- Academy worker redeployment `80315718-7104-4bdd-a027-c563b6bd0703` reached `SUCCESS`.
- Jobs 75-79 were reprocessed into `04 Parsed`; jobs 78 and 79 produced Torah daily progress rows.
- Older jobs 64-74 received dry-run backfill only. Applying older rows remains blocked until a progress-only backfill path exists, because force parsing would duplicate tasks/class notes/accountability rows.

Recovery evidence: `ops/drive-audits/2026-06-23T09-31-drive-class-intake-recovery.md`

## RAW-20260623-002 - Operator laptop bootstrap package

Raw operator request:

> I need a program for my laptop that I could just download and get my whole ENV connected to the whole repo and just so anything I do on my laptop will sync to the GitHub and sync to this desktop machine. Develop whatever piece of code that is that I could just install it in my laptop. Tell Codex right now in a prompt that he should just email it to me at SDRATLR, something that I could just download and just run like one time, and it will just download the entire GitHub repo, all the folders, the one-time folders, you know, everything in the CLI, install like everything that we have installed on our machine to run the software that I'm running on my desktop machine. I need to put onto my laptop. So he should just make this prompt for Codex to make me this full package, and he should email it to me so I could just, you know, click one thing, download it, and install it on my laptop.

Distilled result:

- Registered a request for a one-click Windows laptop bootstrap package that installs/checks BNA local tooling, clones or updates the GitHub repo, prepares local launch/doctor/smoke helpers, and syncs laptop/desktop work through GitHub.
- Existing secure Operator Setup and the older one-click installer builder were inspected as the baseline.
- The safe path is no-secret by default; real env/secrets require the existing encrypted one-time Operator Setup export, explicit approval phrase, and separate passphrase handling.
- Email delivery is blocked until Shloimie confirms the exact recipient address and approves the sender, recipient, subject, body, and package/link. `SDRATLR` is not a complete email address.

Tracking:

- Raw input: `raw-input/RAW-20260623-002-operator-laptop-bootstrap-package.md`
- Register/prompt: `tasks-pending/2026-06-23-operator-laptop-bootstrap-package.md`

Implementation follow-up:

- Operator confirmed delivery to `SDRATLER@gmail.com` and asked Codex to do
  everything.
- Built safe no-secret package
  `install-packages/BNA-Operator-Laptop-Safe-20260623-151349.zip`.
- Added `scripts/build-operator-laptop-installer.ps1`, `scripts/Sync-BNA.ps1`,
  `tests/operator-laptop-installer.test.js`, docs, and package script
  `npm run operator:laptop:package`.
- Verification passed: focused test 4/4, package build, ZIP inspection,
  PowerShell parse of builder/installer/sync scripts, and tracked secret audit.
- Emailed package to `SDRATLER@gmail.com`; Gmail message ID
  `19ef46765f76b97f`.
- Evidence: `ops/operator-setup/2026-06-23-laptop-installer-email.md`
- Follow-up delivery: uploaded the same ZIP to Google Drive folder
  `BNA V2 / Operator Setup Packages`, shared it with `SDRATLER@gmail.com`, and
  emailed the Drive link. Drive file:
  `https://drive.google.com/file/d/1qTR-bfus2O7kDnn0QleQdRji_msmw2w0/view?usp=drivesdk`;
  Drive-link Gmail message ID `19ef4b152d98adbc`.

## RAW-20260623-003 - Provider portal password invalid

Raw operator request:

> why is the pasword not working it sais invalid from provider portal

Distilled finding:

- Provider Portal credentials are separate from private Operations/operator
  credentials.
- The laptop/operator env credentials should be used at `/operations-login.html`
  or `/operations`, not `/provider`.
- `/provider` accepts only provider-specific accounts whose password was set
  through the provider setup-token flow.
- If Shloimie wants a specific provider login reset, Codex needs the target
  provider account/workspace and should generate or resend a provider setup
  link through the approved provider-password flow.

Tracking:

- Raw input: `raw-input/RAW-20260623-003-provider-portal-password-invalid.md`

## RAW-20260623-004 - Operations login from any portal screen

Raw operator request:

> Just make it that I can go to my website and log in from any portal with that username and password. That's what I'm trying to do on my other machine.

Distilled result:

- Registered `REQ-20260623-009` for using a valid Operations identity from any
  parent, student, or provider login screen.
- Implemented a local server fallback that recognizes Operations credentials,
  creates an Operations session, and returns an Operations redirect without
  creating a parent/student/provider session.
- Updated provider, student, parent password, and parent access-code login pages
  to follow the Operations redirect response.
- Verification passed: `node --check server.js` and focused portal/auth tests
  44/44.
- Deployed a narrow Railway hotfix from the running live bundle:
  `a60bb082-94a7-4cbd-84a7-898b1b46ec7b`.
- Railway doctor passed.
- Live Provider Portal smoke passed for username label `shloimie`: status 200,
  `portal_redirect: true`, `redirect_to: /operations`, Operations cookie set,
  provider cookie not set.

Tracking:

- Raw input: `raw-input/RAW-20260623-004-super-admin-universal-portal-login.md`
- Register: `tasks-pending/2026-06-23-super-admin-universal-portal-login.md`
- Live proof:
  `ops/live-smokes/2026-06-23T17-43-portal-ops-login-fallback-live-smoke.md`

## RAW-20260623-005 - Telegram and website assistant control-plane addendum

Raw operator request:

> FOLLOW-UP ADDENDUM - Universal Natural-Language Control Plane for Telegram
> and the Website Assistant. Treat both prompts as one Goal Mode program.
> Do not create another Telegram architecture, website-bot action system,
> action registry, intake pipeline, agent queue, provider onboarding system,
> or execution run. Scope this for family/BNA, One Time, service providers,
> parent/student where enabled, and super admin. Replace the separate
> service-provider onboarding forum/form with the assistant-led onboarding
> spot and use parsed One Time class/Vimeo/upload-folder content for the
> Mishnah class workflow.

Distilled result:

- Registered the addendum as `RAW-20260623-005`, linked to existing issue #7
  parent `PARENT-20260622-001` and GitHub comment `4780321517`.
- Created the dated register
  `tasks-pending/2026-06-23-telegram-website-control-plane-addendum.md`.
- Updated the active run source metadata and handoff to keep the addendum
  attached to the same parent source, without creating another execution run
  or duplicate system.
- Current sequencing: PR #5 is merged, but the local active run remains open
  and dirty. Continue the active run's overlapping workspace/roles/security
  batch `REQ-20260619-303`; start full issue #7/addendum implementation after
  clean sequencing or an explicit pause.

Tracking:

- Raw input:
  `raw-input/RAW-20260623-005-telegram-website-control-plane-addendum.md`
- Register:
  `tasks-pending/2026-06-23-telegram-website-control-plane-addendum.md`

## RAW-20260623-007 - Super Admin One Time Rabbi workspace view

Raw operator request:

> Is there a path that I can go from my super admin to view the one-time Mishnah class? And also, I don't need that other... platform suit I don't know what that is I don't I don't think that's necessary I want to be able to see what he see it and to go into his account I'm super Avenue to go in and view it from his view not from the super adventure view only

Distilled result:

- Added a Super Admin `View One Time as Rabbi` action in Operations that
  switches to the One Time workspace view without creating another credential
  or impersonated provider/student/parent session.
- The live path is
  `/operations?workspace=rabbi_sheller_provider&view=service_providers&section=schedule`.
- Hid the confusing `Platform Suite` entry from normal Operations navigation
  while keeping the old route available for compatibility.
- Deployed the scoped Railway hotfix as
  `dd09456c-ba4e-4dc8-a15e-bffc5034c9db`.
- Live browser smoke passed with Super Admin credentials: clicked the action,
  landed on the One Time `7:00 Class` view, saw `Back to Super Admin`, and
  found zero visible Platform Suite nav buttons/tabs.

Tracking:

- Raw input: `raw-input/RAW-20260623-007-super-admin-one-time-view.md`
- Register: `tasks-pending/2026-06-23-super-admin-one-time-view.md`
- Requirements: `REQ-20260623-038`, `REQ-20260623-039`
