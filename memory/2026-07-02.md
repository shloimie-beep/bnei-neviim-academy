# 2026-07-02 Memory

## RAW-20260702-005 - One Time Clean Integration From PR #62

Shloimie authorized Codex to continue safe launch setup work without waiting on
the conflict-dirty PR #62. The clean path is to use PR #62 as source material,
create a fresh branch from current `origin/master`, port only narrow setup and
readiness artifacts, validate, push a clean PR, and leave exact operator tasks
for remaining external setup.

Decisions preserved:

- PR #62 must not be force-merged while conflict-dirty.
- `info@onetimeonetime.com` is the confirmed One Time sender/reply-to.
- `sdratler@gmail.com` is authorized for seed/test email and internal failure
  alerts after final live links exist.
- Temporary launch domain is `join.onetimeonetime.com`.
- Apex/root `onetimeonetime.com` remains untouched.
- Separate One Time Railway service/database remains preferred.
- No bulk campaign send, live Stripe payment, paid-user cancellation, apex/root
  DNS mutation, hard delete, raw private-data export, secret exposure,
  GHL/LeadConnector runtime, or WhatsApp broadcast is authorized.

Execution note:

- Clean branch `codex/one-time-clean-integration-20260702` was created from
  current `origin/master`.
- Added no-write One Time setup readiness checker and focused tests.
- Restored selected launch-unblocker artifacts and post-setup packet.
- Corrected One Time provisioning target from `app.onetimeonetime.com` to
  `join.onetimeonetime.com`.
- Safe checks passed; setup checker reports `0/8` external setup areas ready.
  The next visible task is to provide the separate One Time Railway
  project/service/environment labels and required non-secret env values.

## RAW-20260702-006 - Background Agent, Drive UI Recording, Mock Data, Railway Provisioning

Shloimie clarified that Codex is authorized to run safe runnable packets, trace
Drive recording work, create TEST/mock review data, and provision the separate
One Time Railway project/service/database if guarded scripts support it.

Execution note:

- Background agent/fleet status was checked. The supervisor is not reliably
  running; readiness is blocked by pointer/branch drift. One safe once-batch
  was attempted and timed out, with task #1736 observed running afterward.
- The newest Drive recording was found and transcribed, but no structured class
  output or UI-correction parse output was available. No raw transcript body was
  committed.
- UI correction packet DAG files were created under
  `ops/prompt-packets/2026-07-02-ui-correction-recording/`.
- Guarded TEST/mock One Time UI-review seed and cleanup scripts were created.
  Dry-run passed; apply remains blocked until a safe One Time DB runtime/alias
  is available.
- Guarded Railway provisioning succeeded for `one-time-production`,
  `one-time-web`, and `one-time-postgres`, including non-secret runtime
  variables and the Railway `DATABASE_URL` service reference.
- Railway custom domain attachment succeeded for `join.onetimeonetime.com`.
  GoDaddy DNS still needs CNAME `join` -> `awaz36ln.up.railway.app` and TXT
  `_railway-verify.join` -> the Railway verification value.
- Setup checker now reports 2/8 areas ready: Railway target and DB service
  reference. The top visible Shloimie task is GoDaddy DNS for the join
  subdomain.

Guardrails held: no bulk campaign, live Stripe payment, WhatsApp broadcast,
apex/root DNS mutation, hard delete, paid-user cancellation, GHL runtime,
secret exposure, private-data evidence leak, email send, Vimeo upload, or Zoom
mutation.
