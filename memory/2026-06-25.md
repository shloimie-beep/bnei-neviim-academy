# 2026-06-25

## RAW-20260625-001 - Parent Return Delay Update

Source: Codex chat.

Shloimie asked Codex to send all parents from the prior trip reminder roster a
return-delay update by WhatsApp and email, using Hebrew for Hebrew-tagged
families.

Message meaning:

- The boys are still at the moshav.
- They are still having a good time.
- Return will be later than expected, probably closer to 3:00.

Raw file:
`raw-input/RAW-20260625-001-parent-return-delay-update.md`

Requirement register:
`tasks-pending/2026-06-25-parent-return-delay-update.md`

Completed:

- WhatsApp sent to 10 deduped parent numbers from the prior roster, 0 failed.
- Email sent to 6 deduped parent emails from the prior roster, 0 failed.
- Hebrew rows were sent to Hebrew-tagged families.
- Live readback confirmed Hebrew message bodies contain real Hebrew and no
  repeated-question-mark corruption.

## RAW-20260625-002 / RAW-20260625-003 - Intake Files And Audio Check

Source: Downloads intake files plus Codex chat follow-up.

Files registered:

- `C:\Users\User\Downloads\13-CODEX-KICKOFF-ISSUE-18-THEN-20.md`
- `C:\Users\User\Downloads\09-FINAL-INTEGRATION-MERGE-DEPLOY.md`

Register:
`tasks-pending/2026-06-25-intake-files-telegram-audio-check.md`

Completed locally:

- Registered both files as raw-intake records with source paths, SHA-256
  fingerprints, parsed intent, and current GitHub truth.
- Confirmed GitHub issue #18 has terminal `NOT SAFE TO APPLY` evidence, and
  issue #20 is closed with merged PRs and live verification.
- Added the durable audio/voice/video ramble rule to the main ramble protocol:
  task/UI/update recordings must be transcribed/preserved, split into canonical
  lanes, and worked with evidence rather than becoming Content/social drafts.
- Added a parser regression proving a voice/audio ramble splits into UI task,
  bot-status task, and separate operator configuration Decision.
- Verified Telegram token metadata without changing credentials: token resolves
  to `bneineviimacademy_bot`, two allowed chat ID aliases are configured,
  webhook is absent, pending update count is 0, and no last Telegram API error
  is reported.

Verification:

- `node --check scripts/telegram-kimi-bridge.mjs`
- `node --check server.js`
- `node --test tests/intake-parser.test.js tests/telegram-media-routing.test.js tests/intake-parser-class-recording.test.js tests/telegram-runtime-status.test.js tests/operations-task-comments-and-dictation.test.js` passed 49/49.

Remaining:

- Local Telegram polling bridge is not currently running. Shloimie said bot
  configuration/startup should be handled separately, so Codex did not change
  credentials or start a long-running bridge.

## RAW-20260625-004 - Drive Transcript Library Sync OAuth Error

Source: Codex chat production/Railway error paste.

Error meaning:

- Content job #83 reached the Drive transcript library sync path.
- It failed before Drive transcript processing because the Google OAuth client
  config at `/app/.secrets/google-oauth-client.json` was invalid for the sync
  script.
- This was an auth/config blocker, not proof that the task parser or
  background task queue had intentionally closed the work.

Register:
`tasks-pending/2026-06-25-drive-transcript-library-sync-oauth-error.md`

Completed locally:

- Repaired `scripts/sync-drive-content-library.mjs` so Railway/local env
  credentials are honored before the OAuth client file fallback.
- Added support for inline `GOOGLE_OAUTH_CLIENT_JSON` in the sync script.
- Changed the script to pass merged `.env.local` plus process env into both
  config loading and OAuth auth construction.
- Hardened `scripts/telegram-kimi-bridge.mjs` so Drive-related bridge actions
  also avoid reading a bad OAuth file when direct env credentials are present.
- Added focused synthetic regression coverage in
  `tests/sync-drive-content-library-auth.test.mjs`.

Verification:

- `node --check scripts/sync-drive-content-library.mjs`
- `node --test tests/sync-drive-content-library-auth.test.mjs` passed 6/6.
- `node --check scripts/telegram-kimi-bridge.mjs`
- Focused Telegram/audio/auth tests passed 44/44.
- `npm run bna:run:validate` passed.

Remaining:

- The fix is not deployed from this dirty worktree.
- Content job #83 should remain open/blocked until the patch is released
  cleanly and Railway has valid Google OAuth refresh-token plus Drive pipeline
  configuration.
