# 2026-07-05

## RAW-20260705-001 - Keyholder live deploy follow-up

- Shloimie clarified that the OpenAI `openaiv2.txt` keyholder file is the
  correct OpenAI key and asked Codex to use available keyholder/Railway keys to
  push PR #87 live.
- Redacted keyholder/Railway readback:
  - OpenAI v2 key is present locally and matches Railway production by
    fingerprint.
  - Vimeo access token is not present in keyholder or Railway; Vimeo client ID
    and client secret are present.
  - Generic Stripe secret is present locally; Railway production now has
    `STRIPE_SECRET_KEY`, `RABBI_STRIPE_SECRET_KEY`, and `RABBI_STRIPE_MODE`
    set by approved keyholder copy with no secret value printed.
  - Railway production already has `DATABASE_URL`, Resend variables, and the
    Rabbi Telegram bot token by redacted readback.
- Operator direction: Telegram worker verification and missing Vimeo token may
  be deferred for the UI deploy; do not claim those integrations are complete.

## PR #87 live deployment

- PR #87 merged to `master` at merge commit
  `3230cf8251b1e416068829eab73d8bdd72ff8f06`.
- Railway production deployment `a6799b08-c6c7-4ba7-b61d-de6e9c81aa25`
  reached `SUCCESS` for that merge commit.
- Live smokes passed for the main BNA app, public Rabbi/One Time landing, and
  the `join.onetimeonetime.com` One Time instance.
- Vimeo access token and full external readback remain deferred, not complete.

## RAW-20260705-003 - Operations Login Blinking Glitch

Shloimie reported that the BNA Operations login page was blinking/glitching and
not letting him log in. Screenshot showed
`/operations-login.html?returnTo=%2Foperations` with the unauthenticated login
form and the BNA Helper bar visible at the bottom.

Parsed requirement:

- `REQ-20260705-007`: stop the Operations login blink/redirect loop and keep
  the unauthenticated login page stable while credentials are entered.

Local verification result:

- Operations login now only auto-redirects already-authenticated Operations
  roles (`super_admin`, `project_owner`, `project_manager`), preventing
  parent/provider/student session cookies from bouncing the browser between
  login and `/operations`.
- The BNA Helper widget is no longer loaded on the unauthenticated Operations
  login page.
- Focused login/auth tests passed 33/33.
- Playwright browser smoke passed with an authenticated parent session mocked:
  the page stayed on login, preserved the typed username, and did not request
  the helper widget.
- Screenshot evidence:
  `ops/playwright-smokes/2026-07-05-operations-login-glitch/local-parent-session-login-stable.png`.

Guardrails: no password capture, no production data mutation, no external
sends, payments, access grants, DNS, or credential changes.

Live deployment result:

- PR #92 merged to `master` at merge commit
  `9327068b85f709b1bf0827f03d71d5a5521d2c79`.
- Railway production deployment `e45e6ca7-5282-4ff2-939c-0d9e4b6ba54e`
  reached `SUCCESS` for the BNA `skillful-motivation` service.
- Live HTML readback for
  `https://bneineviimacademy.org/operations-login.html?returnTo=%2Foperations`
  confirmed the role-gated Operations session check is present, the old
  any-authenticated-session redirect condition is absent, and the login page no
  longer includes `/js/bna-bot-widget.js`.
- Live Playwright smoke with `/api/bna/auth/me` mocked as an authenticated
  parent session stayed on the login page, preserved the typed username,
  recorded one main-frame navigation, and showed/requested no BNA Helper widget.
- Live screenshot evidence:
  `ops/playwright-smokes/2026-07-05-operations-login-glitch/live-parent-session-login-stable.png`.

## RAW-20260705-002 / RAW-20260705-004 - Telegram Fleet And Google Auth Repair

The dirty checkout had used `RAW-20260705-001` for the Telegram/Google audit,
but `origin/master` already used that ID for the PR #87 keyholder deploy
follow-up. During release cleanup the Telegram/Google audit was renumbered to
`RAW-20260705-002`, and the follow-up repair request was renumbered to
`RAW-20260705-004`.

Local code repairs preserved in release branch
`codex/dirty-worktree-release-cleanup-20260705`:

- Agent-fleet readiness resolves the active execution run from
  `ops/execution-runs/latest.json`, synthesizes the agent-fleet lane from active
  requirements when needed, and reports missing upstream as structured
  `branch_has_no_upstream`.
- Google configured checks use the shared `.secrets`-aware OAuth loader for
  provider status, integration readiness, Drive status, and OAuth start.
- Verification passed in the clean release worktree: syntax checks, focused
  Telegram/Google/fleet tests, and `npm run agent:fleet:readiness -- --json`
  with `ok: true`.

Runtime/account actions remain guarded: exactly one Telegram worker/poller
verification/restart and Google reauthorization or hosted worker env changes
still need exact approval if needed.

## RAW-20260705-005 - ChatGPT Dropoff Fleet Claim Completion Fix

Preserved the verified ChatGPT dropoff fleet claim fix from the dirty checkout
under renumbered raw ID `RAW-20260705-005`. The supervisor now hydrates
observable linked tasks through `/api/bna/tasks/:id`, ignores non-queued jobs
for claimability, prioritizes ChatGPT dropoff packet tasks, resolves claimed
linked tasks directly, and treats Telegram notification failures as non-fatal.

Verification in the clean release branch: syntax check passed, focused
agent/dropoff tests passed, and final evidence is stored under
`ops/chatgpt-ramble-dropoff/pickups/2026-07-05-fleet-claim-completion-fix.md`.

## RAW-20260705-006 - OneTimeOneTime Landing Signup Funnel

Preserved the public `/one-time` landing-page replacement as
`RAW-20260705-006`: black/white/yellow signup funnel, safe `#start-free` CTAs,
existing interest form, focused config/action-registry/test updates, design
reference package, Product Quality Compiler packet, and local screenshot
evidence.

Clean release verification passed: Product Quality Compiler validation, focused
One Time tests, canonical One Time local journey smoke, and action watchdog
with zero findings. Protocol drift still reports the older helper-bot ChatGPT
packet at
`ops/prompt-packets/2026-07-03-helper-bot-workspace-agent-chatgpt/README.md`;
that drift predates this release and is not caused by the landing funnel.

## RAW-20260705-007 - Clean Dirty Worktrees And Deploy Everything

Created a clean release worktree at
`C:\Users\User\BNA-release-cleanup-20260705` on branch
`codex/dirty-worktree-release-cleanup-20260705` from `origin/master` because
the original dirty checkout was on merged PR #90 with its upstream branch gone
and was behind current master. Safe work was copied selectively into the clean
branch instead of deploying the stale checkout wholesale.

Initial release cleanup verification:

- Syntax checks passed for `server.js`, `scripts/agent-fleet-readiness.mjs`,
  `scripts/agent-fleet-supervisor.mjs`, and
  `scripts/chatgpt-dropoff-comment-collector.mjs`.
- Focused tests passed; the only initial failure was missing Playwright in the
  fresh worktree, then the Job 101 Playwright smoke passed after using the
  existing dependency tree.
- `npm run bna:run:status` and `npm run bna:run:validate` passed.
- `npm run secrets:audit` passed with 0 tracked secret-risk files.
- `git diff --check` passed after trimming one generated markdown EOF blank
  line.

Final release cleanup closeout:

- PR #94 merged the safe dirty-worktree cleanup batch at
  `a47b4e68024b82017874c76ad8c984785a93f361`; Railway deployment
  `16f668d8-8cec-4319-8b3e-5aa0319dfc7e` reached `SUCCESS`.
- PR #95 fixed the One Time email-review live-smoke copy gap at
  `cbfd5a1fd07af92bfa7edd57a7b023ed301d69dc`; Railway deployment
  `e99f7921-5691-4b83-b5a9-1c7dfd787f71` reached `SUCCESS`.
- Live proof passed for the broad app smoke, Rabbi / OneTime landing, One
  Time shared review, One Time CRM contacts UX, mobile assistant keyboard, and
  the separate `https://join.onetimeonetime.com` instance.
- No checkout session, charge, payment link creation, member creation, access
  grant, live send, DNS change, Drive write, external CRM write, provider
  account mutation, or database cleanup mutation was performed.

## RAW-20260705-008 - Release Captain And One Time UI Recovery

Shloimie said he is not a programmer and needs the repo set up so he can keep
giving natural corrections while Codex ships them safely. He also reported that
the live Rabbi Scheller / One Time Mishnah class UI still looks wrong after the
dirty-worktree cleanup, especially the side panel/navigation where expected
information is missing and only a couple items show a blue branch-like signal.

Registered as `tasks-pending/2026-07-05-release-captain-onetime-ui-recovery.md`
with requirements `REQ-20260705-401` through `REQ-20260705-406`: release truth,
Release Captain workflow, current-state Rabbi/One Time visual audit, focused UI
repair, clean GitHub/Railway deploy/live-smoke, and external-provider blocker
recording.

Guardrails: no live payment, checkout creation, access grant, real campaign
send, WhatsApp send, DNS change, Drive write, credential change, provider
account mutation, production data cleanup mutation, or external CRM write is
approved by this ramble.

Local implementation checkpoint:

- Added a read-only Release Captain command/report so Codex can say whether a
  branch is dirty, unpushed, blocked by release gates, or ready for PR/merge
  without asking Shloimie to inspect git.
- Recovered the One Time Operations side panel by expanding the Rabbi/provider
  modules to include the expected One Time views, restoring the current-section
  subnav, demoting unrelated platform-support labels, and moving One Time CSS
  tokens back to black + yellow instead of stale blue/teal.
- Local proof passed: Product Quality Compiler validation, Release Captain
  unit test, One Time brand/IA/UI smokes, action watchdog with zero findings,
  protocol drift watchdog with zero findings, secrets audit, BNA run status and
  validation, `git diff --check`, and full `npm test` at 1515/1515.
- This is not live proof yet. Remaining publish work is commit, push, PR,
  merge, Railway deploy, and live smoke on the deployed app.

Final live closeout:

- PR #97 merged to `master` at
  `e405bfe484db6515ccc52d4d9913938ee9e0d633`.
- Production `https://bneineviimacademy.org/operations.html` readback contains
  the new One Time sidebar keys `live_class_schedule`, `program_schedule`,
  `community_questions`, `reporting_readiness`, `connector_setup`, and
  `renderSidebarSubnav()`.
- Production `one-time-operations.css` readback contains black/yellow One Time
  tokens and no stale `#0b9fc9`, `#08779c`, or `#b8dff0` literals.
- `npm run app:smoke:one-time-shared-review` and `npm run app:smoke` passed
  against `https://bneineviimacademy.org`.
- Railway deployment-ID readback is not available from this shell because the
  current Railway token/status resolves the separate `one-time-production`
  project and BNA service linking is unauthorized. The deployed-code proof and
  live smokes passed; only the Railway deployment-ID audit remains a tooling
  access gap.

## RAW-20260705-009 - One Time Canonical Target Routing Correction

Shloimie corrected the target for OneTimeOneTime public landing work: the
canonical production target is `https://join.onetimeonetime.com/one-time/` and
possibly `https://join.onetimeonetime.com/`, not only the BNA-hosted
`https://bneineviimacademy.org/one-time/#watch` preview. Any future claim that
One Time public landing work is done must verify the join domain directly.

Registered as
`tasks-pending/2026-07-05-onetime-canonical-target-routing.md` with
requirements `REQ-20260705-501` through `REQ-20260705-510`. Guardrails: no
Postgres deletion, production data mutation, sends, payments, access grants,
DNS changes, Drive writes, credential changes, provider-account mutations,
external CRM writes, or broad stale PR merges.
