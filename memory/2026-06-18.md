# 2026-06-18

## Codex Raw Intake: Mobile Operations, Workspace Isolation, And Public/PWA Guardrails

- Raw input: `RAW-20260618-001`.
- Source channel: `codex_chat`.
- Source file:
  `C:\Users\User\Downloads\CODEX_SUPER_PROMPT_BNA_2026-06-18.md`.
- Repo fallback raw storage:
  `raw-input/RAW-20260618-001-codex-super-prompt-mobile-workspace-audit.md`.
- Requirement register:
  `tasks-pending/2026-06-18-mobile-operations-workspace-audit.md`.
- Active Codex goal:
  Execute the 2026-06-18 BNA mobile-first Operations, workspace isolation,
  public/PWA guardrails, scoped modules, helper, Hebrew, tests,
  documentation, and deployment correction packet through terminal requirement
  statuses with evidence.

### Distillation

The packet asks Codex to implement, not merely analyze, a full mobile-first
Operations and workspace-isolation pass. The highest-priority issues are:

- keep public, parent, and Operations PWA identities separate so installed
  Operations never opens the public site and public browsing never opens
  Operations;
- remove public Operations login from the main public navigation and update
  provider/parent CTA language;
- prove manifests, service worker behavior, start URLs, headers, public hero,
  and launch routing with automated tests;
- enforce workspace and student isolation server-side across APIs and UI;
- make the Operations shell, workspace selector, task manager, calendar,
  modules, helper, Hebrew UI, and seed/test data coherent and scoped.

Initial preflight notes:

- The active Operations runtime is still the Express/static route:
  `/operations` in `server.js` serving `public/operations.html`.
- There is no active `src/app/operations` tree in the current repo; the old
  React TaskApp exists only under
  `docs/archive/dormant-next-supabase-app/src/app/operations/`.
- Current manifests are separate files, but `public/operations-manifest.json`
  and `public/parent-manifest.json` still use broad `/` scope.
- `public/index.html` and shared `public/js/bna-site-nav.js` still expose
  Operations login in public navigation, which conflicts with this packet.
- The latest 2026-06-17 queue/readiness sweep reported active machine tasks
  `0`, observable jobs `0`, and deployment `ca0075c2-5ce1-4a70-b6c8-e8d2c116adae`.
  The pending developer tester ticket-capture brief should not be buried by
  this new packet.

### First Implementation Batch

Public/PWA guardrails were implemented locally for `REQ-20260618-003` through
`REQ-20260618-006`, with related public copy work for `REQ-20260618-021`.

- Operations and parent manifests now use private scopes and distinct SVG icon
  assets.
- The public service worker no longer app-shell caches parent/Operations
  manifests and bypasses private app prefixes.
- Public homepage/shared nav no longer expose Operations login.
- Provider CTA copy now says "Advertise your program for free"; the parent
  page links to `/parent/login?onboard=accountability` with "Sign up and start
  using the app" and six-month early-user copy.
- Verification passed: focused public/PWA tests, Hebrew RTL audit,
  `npm test` (`747/747`), in-app Browser guardrail smoke, true 390px Playwright
  mobile smoke, and `npm run railway:doctor`.
- Deployment/live smoke is not complete: the Railway redeploy script packages
  the current local tree, and this worktree contains extensive unrelated dirty
  and untracked changes. A scope-safe deploy decision or clean deploy bundle is
  the next release blocker.

### Workspace Foundation Slice

Workspace model guardrails were tightened locally for `REQ-20260618-007`, with
partial Operations selector work for `REQ-20260618-009`.

- Server-side workspace directory normalization now defines canonical tenant
  types as `school`, `service_provider`, and `family`, with `super_admin` as a
  context and legacy aliases mapped through compatibility rules.
- Universal assistant workspace seed/backfill now stores One Time as
  `service_provider` and family/household-compatible inputs as `family`.
- The Operations workspace fallback list no longer shows a `Family Directory`
  workspace card.
- The Operations workspace selector no longer has an `All` workspace-type
  button, no longer says "workspaces loaded", and only shows workspace search
  when there are more than eight options.
- Verification passed: focused workspace/Operations tests, `npm test`
  (`747/747`), in-app Browser Operations selector smoke, and true 390px
  Playwright Operations smoke.
- Remaining blocker: existing SQL constraints still allow compatibility values
  so production rows are not broken; a data-safe backfill/constraint migration
  plus deploy/live smoke is still required before the workspace-model
  requirement can be marked fully done.

### Workspace/Student Isolation Slice

The first server-side isolation slice for `REQ-20260618-008` was implemented
and locally verified.

- Added `assertProjectOwnedRowsAccess` in `server.js` for scoped multi-record
  ownership checks.
- The Operations accountability list now calls `assertStudentAccess` when a
  `student_id` filter is provided, so scoped logins cannot request another
  workspace's student ID and receive an ambiguous empty list.
- Bulk content generation now asserts every selected `bna_content_jobs` ID
  before reading jobs, generating drafts, or updating job statuses.
- The tightened routes preserve `err.statusCode` so access failures return
  `403` instead of looking like server failures.
- Regression coverage was added to
  `tests/workspace-person-household-provider-contract.test.js`.
- Verification passed: `node --check server.js`, focused workspace/Operations
  tests, and full `npm test` (`748/748`).
- Remaining blocker: this is local proof only. Broader negative API/live smoke
  coverage and deploy/live proof remain open until a scope-safe deploy path is
  available.

### Operations Shell And Card-System Slice

The first shell/design-system slice for `REQ-20260618-009` and
`REQ-20260618-010` was implemented and locally verified.

- Added a role-scoped `MODULE_TOOLBAR_PRIORITY` and main Operations module
  toolbar in `public/operations.html`.
- The toolbar order is Decisions, Tasks, Calendar, Students, Content,
  Community, Accounting, Automations, Users, and Integrations. Decisions opens
  the Decisions lane inside Tasks.
- On desktop, toolbar buttons fill the available width evenly; on mobile and
  tablet widths, the toolbar owns its own horizontal scroll strip.
- Added shared Operations card tokens (`--ops-card-surface`,
  `--ops-card-border`, `--ops-card-shadow`, `--ops-status-accent`) and applied
  the parchment/light-card surface to the main Operations card families.
- Verification passed: focused shell/brand/workspace tests, full `npm test`
  (`748/748`), and local Playwright smoke at 360, 390, 768, and 1440 widths.
- Proof:
  `ops/playwright-smokes/2026-06-18-operations-module-toolbar-local/report.md`
  plus viewport screenshots in the same folder.
- Remaining blocker: this is local proof only. Broader shell/task/calendar
  simplification and deploy/live proof remain open.

### Task / Intake / Internal Calendar Slice

The first task-manager, intake-routing, and internal-calendar slice for
`REQ-20260618-011`, `REQ-20260618-012`, and `REQ-20260618-013` was implemented
and locally verified.

- The visible task lanes now read Decisions, Tasks, Codex Queue, Blocked,
  Calendar, and Done / Activity.
- Owner filters now use operator-facing people choices: Me / Shloimie, Rabbi
  Elie Scheller, Other team, Unassigned, and External blocker.
- The main Tasks view no longer renders the Agent Work or Queue Health
  diagnostic panels; diagnostics remain technical plumbing outside the daily
  task board.
- Intake Review / Review Queue were removed from workspace navigation, and old
  intake/parser aliases now route into Tasks/Decisions.
- Recording/manual parse flows now return the operator to Decisions with a
  concise notice instead of opening the old standalone Review Queue module.
- Calendar overview is internal-first: it shows Internal Events, removes Google
  Sync from calendar tabs/metrics, removes the task-calendar Google dry-run
  button, and avoids Google Calendar/Classroom copy in the main Calendar page.
- The replacement Internal calendar navigation button is registered as
  `open_internal_calendar` in the detailed action registry artifacts.
- Verification passed: `node --check server.js`, focused task/intake/calendar
  tests (`65/65`), full `npm test` (`748/748`), and local Playwright smoke at
  `390x844` and `1440x900`; `npm run watchdog:actions` also passed with
  `finding_count 0`.
- Proof:
  `ops/playwright-smokes/2026-06-18-operations-task-calendar-intake-local/report.md`
  plus viewport screenshots in the same folder.
- Remaining blocker: this is local proof only. Backend/live idempotency proof,
  broader task data cleanup, module-specific scoping, and deploy/live smoke
  remain open until a scope-safe deploy path exists.

### Community / Content / Live Classes Module Scoping Slice

The first module-scoping slice for `REQ-20260618-014`, `REQ-20260618-015`, and
`REQ-20260618-016` was implemented and locally verified.

- Community read routes and UI calls now honor the active workspace/project
  filter. BNA Community no longer uses hard-coded One Time wording, badges, or
  API calls, while Rabbi/provider context still shows the One Time Community.
- Content/Research now uses the active workspace filter for content jobs,
  class sessions, and project meetings. BNA hides the One Time Library tab and
  does not load One Time-only classroom/question APIs unless the context is
  global or One Time.
- Reusable content lists filter out private/operator/accountability-style
  non-content jobs instead of treating them as teaching material.
- Live Classes members/sessions now accept the active workspace/project filter.
  BNA Live Classes shows workspace-specific copy and no Member Portal button;
  One Time/provider context keeps the member portal affordance.
- Verification passed: `node --check server.js`, focused module/community/
  content/live-class tests, wider workspace/content/communications suites, full
  `npm test` (`752/752`), and local Playwright module-scoping smoke.
- Proof:
  `ops/playwright-smokes/2026-06-18-module-scoping-local/report.md` plus
  mobile/desktop screenshots in the same folder.
- Remaining blocker: this is local proof only. Drive/live folder configuration
  audit, the remaining module-scope requirements, and deploy/live smoke remain
  open until a scope-safe deploy path exists.

### Admin / Communications / Integrations / Automations Scoping Slice

The admin, communications, integrations, and automations scoping slice for
`REQ-20260618-017` was implemented and locally verified.

- Operations now sends the active workspace/project filter to people/users,
  contact communications, communications integration drafts, DNS setup tasks,
  automations, and integration-status calls.
- Server communication list/detail/write paths apply requested project scope,
  and communication writes stamp `project_key` and `workspace_key` metadata.
- Social/email drafts now store and list by workspace/project metadata, and
  schedule/send confirmation checks reject scoped users when draft metadata is
  outside their authorized workspace/project.
- DNS setup tasks now write a `workspace_id` and filter list/update calls by
  workspace access.
- The people/users API now applies selected project membership filters instead
  of only relying on scoped-login defaults.
- Global Integration Readiness is hidden outside Platform/Super Admin context;
  BNA and One Time workspaces keep the scoped connector surfaces.
- Verification passed: `node --check server.js`, focused admin/
  communications/integrations/automations tests (`32/32`), wider related suites
  (`56/56` and `81/81`), `tests/operations-people-filter.test.js` (`5/5`),
  full `npm test` (`754/754`), and local Playwright smoke.
- Proof:
  `ops/playwright-smokes/2026-06-18-admin-comms-integrations-local/report.md`
  plus mobile/desktop screenshots in the same folder.
- Caveat: the local smoke server logged the existing
  `bna_workspaces_type_check` warning for a `family` row during startup. That
  remains tied to the `REQ-20260618-007` production backfill/constraint cleanup
  blocker; the smoke itself passed and the server was stopped afterward.
- Remaining blocker: this is local proof only. Deploy/live smoke remains open
  until a scope-safe deploy path exists.

### Student Detail / Goal Board / Identity Scope Slice

The student detail, accountability, device, assignment, Goal Board, and
identity-matching slice for `REQ-20260618-018` was implemented and locally
verified.

- Operations now sends the active workspace/project filter to student rosters,
  assignments, devices, device access rules, accountability, Torah summaries,
  and group-goal calls. When the operator is inside a selected student detail
  section, the detail-data calls also include the selected `student_id`.
- Server student/accountability/device/group-goal routes now apply requested
  project scope helpers and selected student/device/project ownership checks
  instead of relying only on UI hiding.
- Goal Board and accountability update routes preserve scoped `403` responses
  instead of flattening access failures into `500` errors.
- Student event and Goal Board matching now prefer linked `student_id` values;
  name/Hebrew alias fallback is limited to unlinked legacy events.
- Verification passed: `node --check server.js`, focused student/identity tests
  (`26/26`), broader related tests (`77/77`), full `npm test` (`759/759`), and
  local Playwright student-detail scope smoke.
- Proof:
  `ops/playwright-smokes/2026-06-18-student-detail-scope-local/report.md` plus
  mobile/desktop screenshots in the same folder.
- Caveat: the final local smoke used a synthetic selected student ID because
  the local BNA roster was empty; the smoke still verified request scoping. The
  startup log also showed the existing `bna_workspaces_type_check` warning tied
  to the workspace-model backfill/constraint blocker.
- Remaining blocker: this is local proof only. Deploy/live smoke and exact live
  duplicate-student/data cleanup audit remain open until a scope-safe deploy
  path exists.

### Student Hebrew / RTL Accountability Slice

The student/accountability Hebrew RTL slice for `REQ-20260618-019` was
implemented and locally verified.

- Student portal topbar labels are now language-driven instead of static
  English.
- Device access state labels now use the student label map instead of trusting
  backend English status labels.
- Daily checkoff weekday labels and next-meeting weekday labels localize when
  Hebrew mode is active.
- Sidebar assignment statuses now use the localized assignment status helper.
- Question, assignment, document, and Goal Board cards were hardened against
  mobile RTL horizontal overflow with logical list padding, breakable links,
  and strict card width constraints.
- The Hebrew audit now explicitly checks student/accountability topbar labels,
  device labels, daily weekday localization, goal filters/statuses, helper
  buttons, question/assignment labels, and known English fallback strings.
- Verification passed: `node --check scripts/audit-hebrew-rtl-ui-labels.mjs`,
  `node --check ops/playwright-smokes/2026-06-15-student-hebrew-rtl-audit-live/run-smoke.mjs`,
  Hebrew label audit, focused portal/Hebrew tests (`32/32`), full `npm test`
  (`759/759`), and strict local Playwright Hebrew/RTL smoke.
- Proof:
  `ops/system-audits/2026-06-18T08-34-10-584Z-hebrew-rtl-ui-label-audit.md`
  and `ops/playwright-smokes/2026-06-18-student-hebrew-rtl-local/report.md`
  plus screenshots in the smoke folder.
- Caveat: the Playwright smoke used synthetic student portal data and blocked
  all write endpoints; this is local proof only.
- Remaining blocker: deploy/live Hebrew smoke remains open until a scope-safe
  deploy path exists.

### Scoped Helper / Action Audit Slice

The scoped helper/action-audit slice for `REQ-20260618-020` was implemented and
locally verified.

- Helper planning/execution/audit paths were inspected: planned actions,
  confirmation-required actions, successful execution, and execution failures
  write canonical helper audit rows with redacted page context, args, result or
  error, workspace/project, role, confirmation, and status metadata.
- Helper audit/action-log read endpoints remain admin/all-scope only.
- Project-scoped helper permission checks now reject explicit workspace
  switching through helper arguments, in addition to the existing project-scope
  checks.
- Added a regression test proving same-project/same-workspace helper actions
  are allowed while same-project/wrong-workspace and wrong-project attempts are
  denied.
- Verification passed: `node --check src/lib/bna/helper/permissions.js`,
  focused helper/assistant tests (`32/32`), action/route-security focused tests
  (`37/37`), `npm run watchdog:actions`, `npm run watchdog:security`, and full
  `npm test` (`760/760`).
- Proof:
  `ops/watchdog-audits/2026-06-18T08-45-watchdog-action-audit.md` and
  `ops/watchdog-audits/2026-06-18T08-46-watchdog-security-routes.md`.
- Remaining blocker: this is local proof only. Deploy/live helper smoke remains
  open until a scope-safe deploy path exists.

### Safe Repeatable Seed / Cleanup Path

The safe repeatable seed/test-data slice for `REQ-20260618-022` was implemented
and locally verified as a dry-run/cleanup path.

- Added `npm run seed:req022`, backed by
  `scripts/seed-req022-test-data.mjs`.
- The harness defaults to dry-run, writes both seed and cleanup SQL, tags every
  fixture with `TEST_REQ022` plus `req022_safe_repeatable_seed_v1`, and refuses
  real database writes without explicit apply/cleanup confirmation phrases.
- Coverage includes school, service-provider, and family workspaces; workspace
  roles; a Hebrew/student fixture; assignments; tasks; decision-as-task;
  internal calendar; content/research; learning community; automation draft;
  and helper action audit rows.
- Proof bundles:
  `ops/seed-runs/2026-06-18-req022-local/report.md` and
  `ops/seed-runs/2026-06-18-req022-cleanup-local/report.md`.
- Verification passed: `node --check scripts/seed-req022-test-data.mjs`,
  focused seed tests (`4/4`), seed dry-run, cleanup dry-run, focused safety
  tests (`20/20`), full `npm test` (`764/764`), `npm run watchdog:actions`,
  and `npm run watchdog:security`.
- Remaining blocker: real database apply/readback/cleanup was not run. It needs
  an explicit safe target database and confirmation phrase.

### Mobile Operations Workspace Audit Local Closeout

The 2026-06-18 mobile Operations/workspace correction packet now has terminal
register statuses with local evidence.

- Register: `tasks-pending/2026-06-18-mobile-operations-workspace-audit.md`.
- `REQ-20260618-023` remains blocked for deploy/live proof and live DB seed
  apply/readback/cleanup, not for additional local implementation work.
- `REQ-20260618-024` is already satisfied: the developer tester ticket-capture
  brief remains present and `TASKS.md` preserves it as active.
- Latest proof: full `npm test` passed `764/764`; action/security watchdogs
  passed; seed and cleanup dry-runs passed; `npm run railway:doctor` passed
  again on 2026-06-18T12:03:40+03:00 for production deployment
  `ca0075c2-5ce1-4a70-b6c8-e8d2c116adae`.
- Final source-of-truth watchdog passed after closeout cleanup:
  `npm run watchdog:audit` severity `ok`, finding_count `0`, report
  `ops/watchdog-audits/2026-06-18T09-11-watchdog-audit.md`.
- No deploy/upload or live DB write was performed. The deploy blocker is still
  the mixed dirty worktree plus no explicit safe release/database target
  decision.

## RAW-20260618-003 - Telegram Bot Stuck Work / Google Auth / Intake Folder

Source: codex_chat

Raw request preserved in:
`raw-input/RAW-20260618-003-telegram-bot-stuck-google-drive-intake.md`.

Registered requirements in:
`tasks-pending/2026-06-18-telegram-bot-stuck-google-drive-intake.md`.

Distilled intent:
- Inspect the last couple days of Telegram bot conversation/captures and find
  work that got stuck.
- Fix safe repo/config issues where possible.
- Explain exactly what operator-side action is needed where local code cannot
  solve the problem.
- Diagnose why Google authorization is reported as unhealthy.
- Diagnose why the Telegram bot intake folder is not working.

Closeout findings:
- Local Google Drive authorization was valid the whole time. The break was in
  the hosted `academy-telegram-worker`: it did not have the Google Drive
  reference variables, and the bridge only knew how to load Drive auth from
  local `.secrets` files.
- Fixed the worker path: the bridge now supports hosted env-based Google OAuth
  and Drive pipeline config; the worker has the Google Drive reference vars;
  the hosted worker was deployed and live-smoked. Current worker deployment:
  `069cff7b-4470-4896-9aad-db5575df8efe`.
- The intake folder now works. Drive audit
  `ops/drive-audits/2026-06-18T14-47-52-309Z-google-drive-audit.md` shows Raw
  Media Intake empty and the three 2026-06-18 files in the processed media
  folder/`03 Transcribed` compatibility stage.
- Repaired the three stalled jobs without re-uploading: content jobs `#72`,
  `#73`, and `#74` are now transcribed and parsed. DB readback showed
  `drive_stage='04 Parsed'` with transcript lengths 5,505, 32,048, and 33,459
  characters.
- Added `node scripts/telegram-kimi-bridge.mjs reprocess-drive-job <job_ids>`
  for targeted future repair of existing Drive-backed content jobs. It updates
  the existing job instead of creating duplicates and records failures as
  blocked notes instead of silently leaving jobs stuck at `ingested`.
- Recent live agent jobs since 2026-06-16 were all completed; no active or bad
  agent jobs were found. Recent Telegram-created task/job pairs 1078/232 and
  1079/233 were completed.
- Verification included focused Telegram tests passing 18/18, full `npm test`
  passing 781/781, Drive audit signed in as `office@bneineviimacademy.org`,
  OpenAI diagnostics PASS, worker heartbeat healthy, and DB readback of the
  repaired content jobs. No raw secrets were recorded.

## RAW-20260618-004 - Erev Shabbos WhatsApp Draft Request

Source: codex_chat

Raw request preserved in:
`raw-input/RAW-20260618-004-erev-shabbos-whatsapp-draft-request.md`.

Distilled intent:
- Draft a short parent-facing Erev Shabbos WhatsApp message.
- Lead with the latest short video summary.
- Add a Parsha connection.
- Summarize last week's learning and include actual student/class questions.

Context inspected:
- Latest transcript index and transcripts `#54`, `#53`, and `#52`.
- Respect/rules/responsibility source sheet from 2026-06-14.
- Captured student-question source map from 2026-06-09.
- Hebcal calendar check for Shabbos June 20, 2026.

Closeout:
- Draft produced in chat only.
- Assumption noted: Diaspora Parsha is Korach; Israel Parsha is Chukas.
- No WhatsApp send, Telegram send, social publish, Drive write, or production
  data mutation was performed.

Correction / follow-up:
- The operator clarified that BNA is located in Beit Shemesh, Israel.
- Future BNA Parsha, Erev Shabbos, zmanim, and school-context assumptions must
  default to Israel unless the operator explicitly asks for a Diaspora
  audience.
- `MEMORY.md` was updated with this durable default.

## RAW-20260618-005 - Telegram Weekly Recordings / Erev Shabbos Parsing Follow-Up

Source: codex_chat

Raw request preserved in:
`raw-input/RAW-20260618-005-telegram-weekly-recordings-parsed.md`.

Registered requirements in:
`tasks-pending/2026-06-18-telegram-bot-stuck-google-drive-intake.md`.

Distilled intent:
- Correct BNA location/calendar defaults to Beit Shemesh, Israel.
- Diagnose why the Telegram bot did not produce the requested weekly Erev
  Shabbos / Parsha / WhatsApp message.
- Verify the last week of recordings placed in Drive are parsed.
- Patch and deploy the bot path where safe.

Closeout findings:
- Root cause for the missed bot response: the Telegram weekly report detector
  did not recognize natural wording such as Erev/Arab Shabbos, this week's
  Parsha, last little video message, what we learned, and WhatsApp message.
- The bridge now routes that wording to weekly report generation, and the
  weekly report prompt tells the model BNA is in Beit Shemesh, Israel.
- Successful auto mixed-recording parses now sync the visible content-job
  `drive_stage` to `04 Parsed`.
- Drive audit `ops/drive-audits/2026-06-18T15-06-16-741Z-google-drive-audit.md`
  showed the June 14-18 recordings in processed recordings / compatibility
  `03 Transcribed` and Raw Media Intake empty.
- Live content-job readback verified jobs `#64`, `#65`, `#67`, `#68`, `#69`,
  `#70`, `#72`, `#73`, and `#74` at `04 Parsed` with transcripts and parse
  payloads.
- Hosted academy Telegram worker deployment
  `c57df355-a5e2-4cfd-a5fe-462356376c34` reached `SUCCESS`; status API and
  Telegram webhook info were healthy.

Verification:
- PASS `node --check scripts/telegram-kimi-bridge.mjs`.
- PASS `node --check scripts/railway-start.mjs`.
- PASS `node --test tests/telegram-runtime-status.test.js
  tests/telegram-media-routing.test.js` 20/20.
- PASS `npm test` 783/783.
- PASS `npm run railway:doctor`.

Guardrails:
- No WhatsApp message was sent.
- No Telegram message was sent.
- No social post was published.
- No raw secret values were recorded.

## RAW-20260618-006 - Telegram OpenAI Primary / Kimi Fallback Fix

Source: codex_chat

Raw request preserved in:
`raw-input/RAW-20260618-006-telegram-openai-kimi-fallback.md`.

Registered requirements in:
`tasks-pending/2026-06-18-telegram-openai-kimi-fallback.md`.

Distilled intent:
- Fix the academy Telegram bot so OpenAI is primary when configured and healthy.
- Keep Kimi as fallback when OpenAI is missing or failing.
- Deploy and verify the hosted worker.

Closeout findings:
- OpenAI diagnostics passed locally through the keyholder `openaiv2.txt`
  source; no raw key was printed or copied into tracked files.
- The hosted academy Telegram worker has both OpenAI and Kimi configured.
- Worker startup now shows
  `ApiPath=OpenAI API (gpt-4.1-mini) -> Kimi API (kimi-k2.6)`,
  `OpenAIKey=yes`, and `KimiKey=yes`.
- Telegram content-generation paths that were still OpenAI-only now use the
  shared provider chain: WhatsApp drafts, Facebook drafts, weekly reports,
  transcript topic inventories, content titles, content draft revisions, and
  image descriptions. Audio/video transcription still requires OpenAI.
- Worker code deployment `d4df557d-c041-4293-add1-e8ccd8f0bc79` reached
  `SUCCESS`.
- Worker provider-order deployment `ae652bb9-572d-4a22-b2e9-ecc9dae5cb9a`
  reached `SUCCESS`.

Verification:
- PASS `node --check scripts/telegram-kimi-bridge.mjs`.
- PASS `node --test tests/ai-provider-selection.test.js
  tests/telegram-runtime-status.test.js tests/telegram-media-routing.test.js`
  24/24.
- PASS `npm test` 784/784.
- PASS `npm run openai:diagnose`, latest report
  `ops/qa-runs/2026-06-18T15-39-48-438Z-openai-diagnostics.md`.
- PASS Kimi API health check, status 200 with reply present.
- PASS Telegram status API, configured with no blockers.
- PASS Telegram `getWebhookInfo`, no webhook URL, pending updates 0, no last
  error.

Guardrails:
- No Telegram message, WhatsApp message, social publish, payment, DNS/account
  change, Drive permission change, or raw secret write was performed.
