# 2026-07-07

## RAW-20260707-001 - One Time mailbox status question

Shloimie asked whether Rabbi can view emails in the inbox yet and whether
emails sent in the interim disappear or are being tracked.

Status readback:

- `RAW-20260706-909` says the One Time CRM mailbox MVP was merged, deployed,
  live-smoked, and provider login credentials were created for Rabbi Elie
  Scheller.
- `RAW-20260706-951` says 9 old Resend received emails for
  `info@onetimeonetime.com` dated 2026-06-29 through 2026-06-30 were
  backfilled through the signed live `/api/resend/inbound` handler into
  `bna_communications`.
- Current no-content live check on 2026-07-07: One Time app smoke passed,
  anonymous mailbox access was rejected, authenticated provider mailbox readback
  succeeded, and the live mailbox showed 15 threads, 16 total messages, 9
  inbound messages, and 7 outbound messages.

Answer given: yes, Rabbi can view the provider mailbox when he has the
`one_time_admin` login; interim messages are not supposed to disappear. They
are stored by Resend and/or captured into first-party `bna_communications` via
the inbound webhook. Historical messages from the earlier webhook gap were
backfilled.

## RAW-20260707-003 - Telegram Codex updates, One Time role UI, student view

Shloimie reported that Telegram reminders/updates about Codex programming work
stopped arriving. He wants the Telegram bot to track what Codex is doing and
send concise updates like what was fixed and what the next step is.

He also clarified the intended product-quality standard: broad requests such
as making the app "million dollar" or asking Codex to act like a UI designer
should compile into obvious product/UI corrections such as equal button sizing,
aligned controls, logical filters, and removal of random/debug-looking
information.

One Time role-scope correction: Super Admin should see setup/readiness
diagnostics from Super Admin, but normal One Time provider account view should
look like a scoped production app, not a Super Admin diagnostics dump.

Student visibility request: Shloimie wants a safe way to log in or view what
students see, with privacy/scope guardrails.

Initial local diagnostics:

- `npm run telegram:kimi:status` reports the BNA Telegram bridge is not running
  and is blocked by a Telegram `409 Conflict` from another `getUpdates`
  request.
- `npm run agent:fleet:status` reports the agent-fleet supervisor is not
  running and has claimable/stale jobs, so it should not be broadly restarted
  without a scoped stale-job/notification plan.

Registered as `RAW-20260707-003` with requirement register
`tasks-pending/2026-07-07-telegram-codex-updates-onetime-role-ui-student-view.md`
and prompt packet manifest
`ops/prompt-packets/2026-07-07-telegram-updates-onetime-ui-access/manifest.json`.
