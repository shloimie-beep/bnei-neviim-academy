# One Time Separate Instance Authorization

Captured: 2026-06-21T21:10:00+03:00
Source: Codex chat operator prompt
Workspace: rabbi_sheller_provider
Project: one_time_mishnah_class

The operator explicitly authorized completing `REQ-20260619-313` by provisioning a separate One Time deployment using the existing Railway account and the existing canonical codebase.

Authorized work includes one separate Railway project, one separate Railway Postgres database, the One Time web service, any required worker service, non-secret configuration, secret propagation through the existing secure local/Railway workflow without printing values, idempotent schema/migration application to the new database, One Time-only system/workspace/test-review seed records, stable app deployment, `app.onetimeonetime.com` custom-domain setup, exact DNS verification-record capture, focused live smoke tests, and closeout of `REQ-20260619-313` if the deployment is verified.

The prompt did not authorize new paid purchases beyond normal existing account resources, root-domain changes, unrelated BNA DNS changes, merging PR #5, bulk email, real card charges, copying BNA private data into One Time, repository deletion, or secret exposure.

If Railway requires a new charge, plan upgrade, or irreversible purchase, only that purchase is paused; all independent code, runbook, database, seed, deploy, verification, and evidence work continues where possible.
