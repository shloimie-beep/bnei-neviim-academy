# CODEX GOAL MODE FOLLOW-UP
## One Time Asset Library, Live Funnel, Vimeo, Email, Stripe, and Secure “View as Rabbi” Workspace

Repository:

```text
https://github.com/shloimie-beep/bnei-neviim-academy
```

Latest observed platform line when this prompt was prepared:

```text
PR #5
branch: codex/agent-control-center-20260619
head observed: 04d93788c48f729001f99c54a67f89ef42cfbe79
```

Re-check the current PR head, active run, deployed commit, Railway state, and working tree before doing anything. Do not assume the observed hash is still current.

---

# 1. EXECUTION MODE

This is **goal-mode execution permission**, not a request for another plan.

Start by reading and obeying:

```text
BNA-START-HERE.md
AGENTS.md
docs/BNA-RAMBLE-TO-DONE.md
ops/execution-runs/latest.json
the active run's NEXT-SESSION.md
issues #7 and #8
PR #5
brand-kit/one-time/*
config/brands/one-time.json
config/service-provider-sites/one-time.json
ops/one-time-mishnah/brand-site-review/*
ops/one-time-mishnah/operator-ui-review/*
docs/integrations/VIMEO.md
docs/integrations/RESEND.md
docs/integrations/STRIPE.md
docs/architecture/workspace-community-provider-role-map.md
src/lib/bna/one-time-role-model.js
```

Then:

1. Register this prompt as one trusted source.
2. Preserve the raw source privately and create/update a canonical requirement register.
3. Continue the current active execution run if appropriate. Create a successor only if the run protocol requires it. Never leave two active runs.
4. Use an isolated branch/worktree or another repository-approved scoped-diff mechanism.
5. Capture the baseline branch, base SHA, dirty files, and file ownership.
6. Do not deploy unrelated dirty or untracked changes.
7. Continue automatically through every safe unblocked requirement.
8. Do not ask Shloimie to choose implementation order.
9. A genuine external-account, legal, rights, privacy, sender-identity, tax, billing-policy, or destructive-action question blocks only the dependent requirement.
10. Do not stop after inventory, planning, mockups, or local-only code. Continue through safe implementation, tests, scoped commit/push, approved deployment, and live verification.
11. Do not merge PR #5 unless separately instructed.
12. Do not create a duplicate task manager, asset manager, integration framework, provider portal, or workspace-role system.

---

# 2. OPERATOR OUTCOME

Deliver one coherent One Time launch slice:

1. Inventory, de-duplicate, label, title, classify, organize, and rank all newly downloaded Rabbi Elie Scheller / OneTimeOneTime images and videos, including two ZIP archives and any matching Rabbi Scheller folders in Downloads.
2. Put the originals and organized working library somewhere obvious and safe on the operator's computer.
3. Store only approved, optimized public derivatives in the repository.
4. Produce contact sheets and a ranked recommendation showing exactly which assets should be used for:
   - hero;
   - hero fallback/poster;
   - worldwide locations slideshow;
   - section backgrounds;
   - Rabbi/founder section;
   - small community-comment avatar;
   - class/video thumbnails;
   - social share image.
5. Replace the current complicated or silly public copy with a simple worldwide Mishnayos signup funnel.
6. Make the public Vimeo hero stream live and reliably.
7. Make the One Time member/classroom Vimeo lesson play inside the site rather than sending a student to a CAPTCHA or raw external page.
8. Complete the safe live-readiness work for email and Stripe using credentials already in the approved keyholder/environment.
9. Add a secure platform-super-admin feature allowing Shloimie to enter the One Time workspace and **see exactly what Rabbi Scheller sees**, with the same role scope, navigation, data filters, and black/yellow One Time theme.
10. Keep One Time backend functions aligned with the shared platform—CRM, members, communications, automations, classes, content, payments, integrations—but present only Rabbi-relevant modules and One Time branding.

---

# 3. CURRENT FACTS THAT MUST NOT BE CONFUSED

## 3.1 Vimeo IDs

The public hero/promo video traced from the legacy OneTimeOneTime site is:

```text
Vimeo ID: 1158542993
hash: daa31d3417
player: https://player.vimeo.com/video/1158542993?h=daa31d3417
title: Promo website
duration: about 32 seconds
```

The member-library/class sample is different:

```text
Vimeo ID: 1178363755
hash: 282ea2577c
title: Pesachim perek 10
duration: about 39 minutes 40 seconds
```

**Do not use `1178363755` as the public hero.**
It is the lesson/library sample.

## 3.2 Existing raw hero source

The traced local source has been:

```text
C:\Users\User\Downloads\OneTimeOneTime - Rabbi Eli Scheller_files\promo_website_v1 (1080p) (1).mp4
```

The repository correctly avoided committing the large raw MP4. Keep that rule. Stream from Vimeo and retain a local archival original.

## 3.3 Existing brand palette

Use the canonical One Time palette:

```text
near black  #080910
charcoal    #10131a
deep navy   #081323
navy 2      #102634
teal        #08779c
cyan        #0b9fc9
yellow      #ede518
cream       #faf9f4
white       #ffffff
muted       #aeb9c6
```

## 3.4 Existing role model

The platform already defines:

```text
platform_super_admin
workspace_owner
workspace_admin
workspace_manager
provider_staff
parent
student
```

Rabbi Scheller is the One Time `workspace_owner`.
Shloimie is platform super admin and also has scoped One Time admin/manager compatibility.

Extend this model safely. Do not create a second role system.

---

# 4. PHASE A — DISCOVER EVERY NEW DOWNLOAD

## 4.1 Source area

Inspect:

```text
C:\Users\User\Downloads
```

Look for all current/recent items associated with these aliases:

```text
Rabbi Elie Scheller
Rabbi Ellie Scheller
Rabbi Eli Scheller
Rabbi Scheller
Rabbi Sheller
Elie Scheller
One Time
OneTimeOneTime
One Time Mishnah
One Time Mishnayos
```

The operator reports:

- two newly downloaded ZIP archives;
- another Rabbi Elie Scheller folder;
- many newly downloaded images;
- video clips;
- previous OneTimeOneTime downloaded-site folders.

Do not assume exact filenames. Discover candidates by:

- path/name aliases;
- archive contents;
- media metadata;
- modification time;
- visual relation to the Rabbi/One Time program.

Do not indiscriminately process unrelated Downloads content.

## 4.2 Supported inputs

Inventory:

```text
.jpg
.jpeg
.png
.webp
.avif
.heic
.heif
.gif
.tif
.tiff
.bmp
.svg
.mp4
.mov
.m4v
.webm
.mkv
.avi
.zip
```

Also inventory HTML-downloaded site folders when they contain relevant assets.

## 4.3 Safe archive extraction

For every relevant ZIP:

1. Record original path, size, modified time, and SHA-256.
2. List archive contents before extracting.
3. Reject path traversal and suspicious executable content.
4. Extract into a local ignored staging area, for example:

```text
<repo>\.runtime\one-time-asset-intake\<timestamp>\archives\<archive-slug>\
```

5. Never extract directly into `public/`.
6. Do not overwrite same-name files.
7. Preserve the original ZIP untouched until the final inventory is verified.

## 4.4 Do not delete first

Do not delete, rename, or move the Downloads originals during discovery.

The workflow is:

```text
discover
-> fingerprint
-> inventory
-> extract safely
-> de-duplicate
-> visually review
-> organize copies
-> verify counts/hashes
-> only then optionally move originals to an archive
```

Any deletion or irreversible move requires a separate confirmation.

---

# 5. PHASE B — BUILD A REAL LOCAL ASSET LIBRARY

Create or reuse an obvious operator-accessible local archive.

Recommended root, unless an established BNA asset archive already exists:

```text
C:\Users\User\Documents\BNA-Assets\One-Time\
```

Required structure:

```text
C:\Users\User\Documents\BNA-Assets\One-Time\
  00-Inbox-Snapshot\
  01-Originals\
    Images\
    Videos\
    Archives\
    Legacy-Site\
  02-Organized\
    Rabbi-Portraits\
    Teaching\
    Stages-and-Venues\
    Worldwide-Locations\
    Students-and-Families-Private-Review\
    Community\
    Press-and-Publications\
    Logos-and-Brand\
    Worksheets-and-Documents\
    Video-Clips\
    Unknown-Needs-Review\
  03-Selected\
    Hero\
    Hero-Posters\
    Location-Slideshow\
    Founder-Section\
    Community-Avatar\
    Library-Thumbnails\
    Social-Share\
  04-Web-Exports\
    AVIF\
    WebP\
    JPEG-Fallback\
    Video-Posters\
  05-Contact-Sheets\
  06-Manifests-and-Reports\
  99-Quarantine\
```

Requirements:

- Preserve originals byte-for-byte.
- Store source path and original filename in the manifest.
- Do not keep secrets in this archive.
- Do not use the BNA keyholder folder for media.
- Make the folder easy for Shloimie to open and find.
- Add a local `START-HERE.html` or `README.txt` explaining the folders and linking to contact sheets.

---

# 6. PHASE C — METADATA, HASHES, DE-DUPLICATION, AND NAMING

## 6.1 File metadata

For every image record:

- source path;
- archive source when applicable;
- original filename;
- SHA-256;
- perceptual hash when available;
- file type;
- width/height;
- orientation;
- color profile;
- EXIF capture date;
- camera metadata;
- GPS metadata;
- file modified date;
- transparency;
- alpha/background behavior;
- duplicate group;
- near-duplicate group.

For every video:

- source path;
- original filename;
- SHA-256;
- file type/container;
- codec;
- duration;
- width/height;
- frame rate;
- audio channels;
- creation metadata;
- rotation;
- bitrate;
- duplicate group;
- near-duplicate/source relationship.

Strip public derivatives of unnecessary EXIF/GPS metadata.
Preserve metadata in the private manifest where useful.

## 6.2 De-duplication

Identify:

- exact byte duplicates;
- resized/cropped duplicates;
- compressed copies;
- screenshot copies;
- video transcodes;
- stills derived from videos.

Never silently discard duplicates.
Choose one canonical original and record all aliases.

## 6.3 Naming convention

Use descriptive, stable, plain filenames.

Image example:

```text
rabbi-elie-scheller-jerusalem-live-event-stage-wide-undated-01.webp
rabbi-elie-scheller-studio-portrait-facing-camera-undated-01.webp
rabbi-elie-scheller-community-comment-avatar-undated-01.webp
one-time-worldwide-location-london-stage-wide-undated-01.webp
```

Video example:

```text
rabbi-elie-scheller-one-time-promo-worldwide-32s-undated-01.mp4
rabbi-elie-scheller-live-mishnayos-class-pesachim-39m40s-undated-01.mp4
```

Naming rules:

- lowercase kebab case;
- no random camera names in final organized copies;
- include verified location/event only when known;
- use `undated` or `location-unknown` rather than inventing;
- no child names in public filenames;
- no private family names;
- preserve original filename in manifest;
- avoid ambiguous words such as `epic`, `final`, `new`, or `good` without meaning.

---

# 7. PHASE D — VISUAL REVIEW AND CONTENT LABELING

## 7.1 Inspect images visually

For every unique image or meaningful near-duplicate, label:

- primary subject;
- number of people;
- Rabbi present/not present;
- child/minor present;
- audience/crowd present;
- location/venue if verifiable;
- event if verifiable;
- shot type:
  - close portrait;
  - half-body portrait;
  - full-body;
  - teaching close;
  - stage wide;
  - audience wide;
  - candid;
  - group;
  - screenshot;
  - logo;
  - document;
- expression/mood;
- visual quality;
- sharpness;
- lighting;
- background quality;
- text overlays;
- crop flexibility;
- safe text area;
- horizontal/vertical/square suitability;
- likely web uses;
- rights/privacy review state.

## 7.2 Inspect videos efficiently

For each relevant video:

1. Run `ffprobe`.
2. Extract contact-sheet frames at meaningful intervals.
3. Detect scene changes when practical.
4. Create a private contact sheet.
5. Identify:
   - strongest opening frame;
   - strongest hero segment;
   - strongest Rabbi close-up;
   - strongest stage/location frames;
   - child/crowd-heavy sections;
   - logos/text that may create rights issues;
   - audio relevance;
   - potential short clips;
   - poster candidates.
6. Do not commit the full raw video to Git.
7. Do not publish a new video without rights/privacy review.

## 7.3 Quality score

Score each candidate from 1–5 for:

- brand fit;
- visual quality;
- authenticity;
- crop flexibility;
- mobile usability;
- desktop usability;
- text-overlay usability;
- privacy/rights safety;
- uniqueness;
- emotional credibility.

Include a short reason, not merely a number.

---

# 8. PHASE E — RIGHTS, CONSENT, AND PRIVACY

Every asset must receive one status:

```text
approved_public
legacy_public_needs_confirmation
rabbi_only_likely_safe
adult_group_needs_review
minor_present_needs_consent
crowd_needs_review
publication_logo_permission_check
private_internal_only
reject_quality
duplicate
unknown_needs_review
```

Rules:

- Do not publish identifiable minors without verified permission.
- Do not assume that a file being in Downloads gives publication rights.
- Do not publish child/crowd imagery merely because the legacy page used it.
- Do not show minor names.
- Do not use private screenshots with browser chrome, emails, account names, or personal data.
- Publication logos may be used only with accurate, non-misleading wording and approved permission/status.
- “As seen across the Jewish world” must not imply sponsorship.
- Keep a rights/consent column and exact blocker.

Assets with unresolved rights can appear in private contact sheets but not the public repo.

---

# 9. PHASE F — REQUIRED ASSET DELIVERABLES

Create private/local:

```text
06-Manifests-and-Reports\asset-inventory.json
06-Manifests-and-Reports\asset-inventory.csv
06-Manifests-and-Reports\asset-inventory.md
06-Manifests-and-Reports\duplicate-groups.json
06-Manifests-and-Reports\rights-review.md
06-Manifests-and-Reports\selection-ranking.md
05-Contact-Sheets\all-images.html
05-Contact-Sheets\video-contact-sheets.html
START-HERE.html
```

Create tracked, redacted repository evidence under a suitable current One Time path, for example:

```text
ops/one-time-mishnah/asset-intake/<date>/
  SUMMARY.md
  SELECTION-MAP.md
  RIGHTS-BLOCKERS.md
  PUBLIC-DERIVATIVES.json
```

Tracked evidence must not contain:

- private raw asset bytes;
- minor names;
- GPS coordinates;
- private Downloads inventory unrelated to One Time;
- secrets;
- private screenshots;
- unapproved identifiable children.

## 9.1 Required ranked selections

Return a ranked shortlist with exact local path, repo derivative path if approved, dimensions, recommended crop, and reason for:

1. Public hero video.
2. Public hero poster.
3. Alternate hero still.
4. Worldwide locations slideshow—8 to 15 best images if available.
5. “As seen across the Jewish world” visual proof section.
6. Founder/Rabbi portrait near the bottom.
7. Small circular/square Rabbi avatar for community comments and replies.
8. Live-shiur section image.
9. Worksheets/accountability section image.
10. Video-library section image.
11. Social Open Graph image.
12. Mobile hero fallback.
13. Email header image.
14. Member-library thumbnail set.

If there are not enough safe, high-quality assets, say exactly what is missing.

---

# 10. PHASE G — USE THE SELECTED IMAGES

Once the inventory and selection map are complete, continue automatically using the top-ranked **approved** assets.

Do not wait for another prompt unless:

- rights/consent is unresolved;
- the top candidates materially differ and require a real brand choice;
- all usable candidates are unsafe.

Create web derivatives:

- AVIF when supported by the current build;
- WebP;
- JPEG fallback where appropriate;
- responsive sizes;
- mobile crops;
- desktop crops;
- square avatar;
- Open Graph image;
- poster images.

Optimize without visible quality loss.

Public destination structure should remain organized, for example:

```text
public/images/one-time/
  brand/
  portraits/
  locations/
  teaching/
  community/
  press/
  video-posters/
  social/
```

Do not create random duplicate copies at the root.

Update:

```text
brand-kit/one-time/assets.json
config/brands/one-time.json
config/service-provider-sites/one-time.json
ops/one-time-mishnah/brand-site-review/asset-manifest.json
```

Preserve compatibility aliases only when required and document them.

---

# 11. PHASE H — REBUILD THE LANDING AS A BASIC FUNNEL

The public page should be simple.

Do **not** lead with complicated language such as:

- worldwide storyteller;
- generic educational platform;
- SaaS suite;
- shared review;
- internal provider review;
- technical integration descriptions.

This is a basic signup funnel for Rabbi Scheller’s worldwide Mishnayos class.

## 11.1 Proposed core positioning

Use plain copy close to this, refining only for grammar and brand voice:

### Top campaign bar

```text
30 DAYS TO JOIN — START WITH 30 DAYS FREE
```

Include a real countdown timer.

### Hero eyebrow

```text
WORLDWIDE LIVE MISHNAYOS
```

### Hero headline

```text
Learn Mishnayos Live with Rabbi Elie Scheller
```

Alternative acceptable headline if it fits the visual better:

```text
Join Rabbi Elie Scheller’s Worldwide Mishnayos Class
```

### Hero subhead

```text
Live shiurim, daily accountability, practical worksheets, and direct interaction with the Rabbi—wherever you live.
```

### Primary CTA

```text
START 30 DAYS FREE
```

### Secondary CTA

```text
WATCH RABBI SCHELLER
```

The secondary CTA should go to a verified TorahAnytime profile/content destination or approved on-site video section. Do not guess the TorahAnytime URL. Resolve it from the downloaded legacy site, current assets, existing content, or an authoritative profile.

## 11.2 Offer

Current desired product direction:

```text
$67 USD per month after a 30-day free trial
```

The public page may state this only after confirming it matches the canonical One Time product configuration.

The trial and campaign are separate concepts:

1. **Campaign signup window:** visitors have a real 30-day enrollment window.
2. **Individual trial entitlement:** an eligible signup receives 30 days free.

Do not make an evergreen fake timer that resets per visitor or page refresh.

## 11.3 Countdown timer contract

Implement a server-backed/config-backed campaign deadline:

```text
ONE_TIME_CAMPAIGN_START_AT
ONE_TIME_CAMPAIGN_DEADLINE_AT
ONE_TIME_CAMPAIGN_TIME_ZONE
ONE_TIME_CAMPAIGN_OFFER_KEY
```

Requirements:

- one canonical deadline;
- server time, not browser-only time;
- no reset on refresh;
- exact date/time visible near the countdown;
- correct timezone;
- accessible text equivalent;
- no flashing;
- reduced-motion safe;
- expired state;
- CTA changes or closes when expired;
- admin can inspect/edit campaign timing through a permissioned setting;
- audit history for changes.

If the exact launch/deadline timestamp is not documented, create one concise Decision and complete all timer infrastructure without inventing a deceptive date.

## 11.4 Funnel page structure

Use this order:

1. Real countdown/campaign bar.
2. Black/navy navigation with white script logo.
3. Full-width Vimeo video hero.
4. Primary signup CTA.
5. Compact “As seen across the Jewish world” strip with approved logos/wording.
6. Simple benefits:
   - Live Mishnayos shiur.
   - Daily accountability.
   - Practical worksheets.
   - Interaction with Rabbi Scheller.
   - Replay/video library.
7. Worldwide locations slideshow/gallery.
8. “How it works” in three steps.
9. Live-stream/class schedule preview.
10. TorahAnytime/content section.
11. Founder/Rabbi section with a strong portrait.
12. FAQ.
13. Final CTA.
14. Legal/privacy/contact footer.

Avoid a wall of equal cards.

## 11.5 Worldwide locations slideshow

Use the best approved location/stage images.

Requirements:

- 8–15 images when available;
- title/caption only from verified metadata;
- no invented city/country;
- manual navigation;
- pause;
- keyboard access;
- swipe support;
- reduced motion;
- responsive crop;
- lazy load;
- no aggressive automatic motion;
- no unapproved children;
- clear message such as:

```text
Teaching and inspiring Jewish communities around the world
```

Do not claim a location unless verified.

## 11.6 Founder section

Near the lower page, use a strong, calm headshot/portrait of Rabbi Scheller.

Suggested heading:

```text
Learn with Rabbi Elie Scheller
```

Suggested short copy:

```text
Clear Mishnayos learning, steady accountability, practical resources, and a live connection to the Rabbi.
```

Do not use exaggerated or outlandish copy.

## 11.7 Community avatar

Create a small optimized square/circular portrait derivative of Rabbi Scheller for:

- community comments;
- Rabbi replies;
- announcements;
- support/reply identity;
- class cards.

Do not reuse a poor hero crop.
Use a dedicated face-centered crop with accessible alt text.

---

# 12. PHASE I — MAKE VIMEO LOAD LIVE

## 12.1 Public hero

Use:

```text
https://player.vimeo.com/video/1158542993?h=daa31d3417
```

Diagnose why it is not loading live.

Check:

- current landing HTML;
- config injection;
- CSP;
- `frame-src`;
- Vimeo privacy/domain restrictions;
- `Referer`;
- iframe `allow`;
- autoplay policy;
- muted state;
- playsinline behavior;
- loop/background settings;
- lazy-loading interaction;
- z-index;
- overlay;
- poster/fallback;
- browser console;
- service worker/cache;
- Railway/deployed static bundle;
- desktop/mobile Safari/Chrome behavior;
- reduced-motion behavior;
- network failure state.

Required result:

- hero plays from Vimeo on the live landing where browser policy allows;
- muted;
- looped;
- inline;
- cover crop;
- no visible player chrome in background mode;
- readable overlay;
- no layout shift;
- fast poster first;
- fallback still on blocked autoplay/network;
- pause/reduced-motion handling;
- no raw local MP4 exposed.

## 12.2 Vimeo provider configuration

The user authorizes the safe integration work needed to make existing One Time Vimeo content load live.

Use only approved credentials from:

- keyholder;
- local ignored `.secrets`;
- Railway environment;
- existing provider configuration.

Never print credentials.

Permitted external Vimeo actions for this goal:

- validate authenticated user/token;
- read account and video metadata;
- verify scopes/plan;
- inspect privacy/embed settings;
- configure approved embed domains for existing One Time videos when safe and required;
- organize existing videos into the approved One Time folder/project if current authorization supports it and no content is deleted;
- update approved poster/thumbnail metadata when reversible.

Not permitted without another explicit instruction:

- delete videos;
- replace source files;
- bulk upload private media;
- publish unreviewed videos;
- expose unapproved child/crowd content;
- change account ownership or billing.

## 12.3 Classroom/member library

Use the proper Vimeo embed/player for:

```text
1178363755 / 282ea2577c
```

Requirements:

- embedded inside the classroom;
- domain/privacy settings permit the production host;
- no external CAPTCHA flow;
- responsive;
- keyboard accessible;
- captions/transcript state visible;
- poster and error state;
- no autoplay for a lesson by default;
- resume/watch-progress integration when supported;
- fallback link only as secondary recovery;
- correct lesson metadata;
- no parent handbook mislink.

---

# 13. PHASE J — GET EMAIL READY AND WORKING

Use the current canonical email architecture.
Do not create a parallel sender.

Inspect:

- Resend credentials;
- Gmail/Google sender path if present;
- One Time sender identity;
- domain;
- DNS;
- webhook secret;
- Railway variables;
- current email templates;
- Communications UI;
- sender/readiness decisions;
- no-send preview state.

The operator states the API credentials already exist in the system. Verify rather than assume.

## 13.1 Resolve the actual sender path

Determine:

- which account owns One Time sending;
- which domain is intended;
- exact `From` name;
- exact `From` address;
- reply-to;
- test recipient;
- production recipient policies.

Prefer existing documented secure values.
Do not invent an email identity.

If the domain/from identity is genuinely missing, create one concise Decision and complete all non-dependent configuration.

## 13.2 Live-readiness work

Complete safely:

- credential normalization/fingerprint diagnostics;
- local config;
- Railway propagation;
- sender/domain status readback;
- DNS-record instructions/readback;
- webhook endpoint;
- signature verification;
- idempotency;
- event logging;
- bounce/complaint handling;
- suppression state;
- template render;
- plain text;
- preview;
- test-send gate;
- no secrets in UI/logs.

## 13.3 Test send

A single real test email is allowed only when all of these are true:

- verified sender/domain;
- approved test recipient already exists in secure config;
- email is clearly labeled a One Time integration test;
- no bulk audience;
- no marketing claim;
- no student/private information;
- send result and provider message ID are logged safely.

If no approved recipient is configured, stop at ready-to-send and report the exact missing variable. Do not choose a recipient from public data.

## 13.4 Public/signup emails

Prepare and verify:

- signup received;
- trial started;
- class access;
- upcoming class;
- payment succeeded;
- payment failed;
- trial ending;
- password/access setup;
- support received.

Do not enable bulk launch email without separate audience approval.

---

# 14. PHASE K — GET STRIPE LIVE-READY

Use the current Stripe architecture and existing approved credentials.

Do not create a parallel payment system.

## 14.1 Canonical offer

Expected offer, subject to confirmation against the product source of truth:

```text
$67 USD/month
30-day free trial
```

Campaign enrollment window:

```text
30 days from the approved campaign launch time
```

## 14.2 Do not guess financial policies

Inspect existing decisions/configuration for:

- Stripe account owner;
- live/test mode;
- card collected at signup or not;
- subscription versus payment link;
- cancellation;
- trial-ending behavior;
- refunds;
- tax;
- receipts/invoices;
- provider payout;
- Stripe Connect.

If a required policy is not decided, create exactly one concise Decision for that policy and continue everything else.

Recommended launch pattern may be presented, but not silently imposed:

```text
Stripe Checkout subscription
payment method collected at signup
30-day trial
$67 USD/month after trial
cancel before trial end to avoid charge
```

## 14.3 Permitted Stripe setup

The user authorizes safe setup to make Stripe launch-ready:

- validate the intended Stripe account and mode;
- normalize and install credentials securely;
- create or reconcile the One Time Product;
- create or reconcile the recurring $67 USD Price;
- create/reconcile webhook endpoint;
- implement Checkout Session creation;
- implement success/cancel routes;
- implement subscription/trial state;
- verify webhook signatures;
- enforce idempotency;
- grant access only after verified Stripe state;
- revoke/review access after cancellation/refund according to policy;
- show billing state in parent/member/admin portals;
- configure Customer Portal if the current architecture supports it and policy is approved;
- record provider IDs without secrets.

## 14.4 Not permitted automatically

Do not:

- create a real customer from an unrelated contact;
- charge a real card;
- run a $1 test charge;
- refund;
- cancel a real subscription;
- create payouts;
- enable Stripe Connect;
- email a real audience;
- migrate real billing records without a reviewed plan.

## 14.5 Verification

Required:

- Stripe test-mode end-to-end checkout;
- 30-day trial creation;
- duplicate webhook idempotency;
- failed/expired checkout no access;
- subscription active access;
- cancellation state;
- payment failure state;
- no cross-workspace access;
- UI correctly distinguishes test and live;
- production config readiness readback;
- no live charge.

---

# 15. PHASE L — SECURE SUPER-ADMIN “VIEW AS RABBI”

Shloimie must be able to enter the One Time workspace and see the backend exactly as Rabbi Scheller sees it.

This is not the current `?review=one-time` fixture.
It must be a secure role-scoped view.

## 15.1 Entry point

Within the One Time workspace, add a clear super-admin-only action in a suitable area such as:

```text
Workspace
-> Users & Access
-> Rabbi Elie Scheller
-> View workspace as Rabbi
```

Alternative location:

```text
Workspace switcher
-> One Time
-> View as Workspace Owner
```

Use the existing One Time role model.

## 15.2 Security model

Requirements:

- only `platform_super_admin`;
- server-side authorization;
- target must be an active member of the same workspace;
- target role is `workspace_owner`;
- no query-param-only impersonation;
- no password exposure;
- no copying Rabbi session tokens;
- short-lived signed view-as session;
- original actor retained;
- target actor retained;
- start event audited;
- end event audited;
- route/API logs record:
  - acting platform admin;
  - viewed-as identity;
  - workspace;
  - timestamp;
  - request ID;
- cannot escape target workspace while in view-as mode;
- target permissions are enforced by the same server-side authorization path;
- no private data beyond what the target can see;
- CSRF protection;
- session expiry;
- explicit exit.

## 15.3 Read-only by default

The default mode is:

```text
VIEWING AS RABBI — READ ONLY
```

Show a persistent banner:

```text
You are viewing One Time as Rabbi Elie Scheller
Exit view
```

In this mode:

- ordinary navigation works;
- all data filters match Rabbi scope;
- external sends/charges/uploads are disabled;
- destructive actions are disabled;
- writes are disabled unless an existing explicit safe preview action is designed for view-as.

Do not silently perform a write as Rabbi.

If the platform later needs “Act as” write support, that must be a separate audited capability with stronger confirmation.

## 15.4 Exact visual experience

The view-as session must show:

- One Time white script logo;
- near-black/navy shell;
- yellow active/primary accents;
- cyan/teal informational accents;
- One Time workspace name;
- Rabbi-appropriate navigation;
- no BNA identity;
- no Webcraft/Platform Suite wording;
- no Codex Queue;
- no Watchdog;
- no internal engineering details;
- no platform-wide workspace selector except the persistent exit-view control;
- only One Time CRM, classes, content, community, communications, payments, integrations, support, tasks, and settings allowed for the owner role.

## 15.5 Super-admin management remains separate

Shloimie must have two clear modes:

1. **Manage One Time as platform admin**
   - platform/admin capabilities;
   - workspace diagnostics;
   - user and integration management.

2. **View as Rabbi**
   - exact Rabbi owner experience;
   - read-only;
   - exact Rabbi scope and brand.

Do not mix these modes.

## 15.6 Required tests

- unauthorized user cannot start view-as;
- workspace admin cannot view as unrelated owner unless allowed by policy;
- BNA user cannot view as Rabbi;
- view-as cannot read BNA private data;
- direct URL without signed session fails;
- target deactivation invalidates view-as;
- session expiration works;
- exit restores original platform session;
- banner persists across navigation;
- APIs enforce target scope;
- writes are blocked;
- audit events exist;
- no target password/token exposure;
- Playwright verifies exact branded navigation.

---

# 16. PHASE M — ONE TIME BACKEND AS A WHITE-LABEL WORKSPACE

Do not fork the entire backend into a new app.

Use the shared platform shell and workspace-aware components.

Apply One Time brand tokens and module visibility through:

```text
config/brands/one-time.json
config/service-provider-sites/one-time.json
workspace membership
role capabilities
module-visibility configuration
```

## 16.1 Rabbi-visible modules

The final owner workspace should be organized around:

```text
Overview
Classes
Schedule
Members
CRM / Leads
Classroom
Video Library
Worksheets
Community / Questions
Communications
Automations
Payments / Access
Integrations
Support
Tasks
Settings
```

Use better plain labels where existing architecture dictates.

Hide from Rabbi:

```text
Watchdog
Codex Queue
Agent internals
raw prompt intake
proof gaps
API contracts
OAuth token details
platform-wide goals
BNA student accountability
BNA private records
Webcraft branding
Platform Suite
developer test fixtures
```

## 16.2 CRM and automations

One Time CRM and automations must be real workspace-scoped views of the shared backend:

- only One Time contacts;
- only One Time leads;
- only One Time parent/member/student relationships;
- only One Time campaigns;
- only One Time class workflows;
- only One Time payment/access flows;
- only One Time support tickets;
- no GHL runtime.

Automations should be shown in plain language:

- Signup received;
- Start trial;
- Grant member access;
- Upcoming class reminder;
- Trial ending;
- Payment failed;
- Payment succeeded;
- Publish class replay;
- New private question;
- Support request.

Do not expose implementation class names or API internals.

---

# 17. PHASE N — FIX THE SPECIFIC AUDIT FAILURES WHILE TOUCHING THESE SURFACES

Do not redo every prior UI audit in this prompt, but fix the defects directly related to the assets, funnel, integrations, classroom, and view-as work:

- Public hero missing Vimeo video.
- Test/review warnings dominating public copy.
- Test portal links visible publicly.
- Student worksheet pointing to Parent Handbook.
- Parent/classroom links not opening.
- Student tab routing to Parent.
- External Vimeo CAPTCHA instead of embedded video.
- Dead classroom navigation.
- Code-style labels with underscores.
- Debug statuses such as `ready_protected_reference`.
- Email preview cards not opening.
- Integration cards showing vague `gated`/`blocked external` states.
- No exact next action on Vimeo/Stripe/email cards.
- BNA copy inside One Time provider/communications.
- Provider portal using generic BNA lockup.
- Unclear Support/Activity combination.
- Internal developer modules visible in Rabbi workspace.
- Wrong/low-contrast One Time colors.
- Excessive pill navigation.
- Missing legal/privacy footer on the public funnel.

Every changed control must be registered in the action registry.
Every changed route must be registered in the route registry.

---

# 18. PHASE O — PUBLIC SIGNUP FLOW

The CTA must lead somewhere real.

Preferred flow:

```text
Landing
-> One Time signup
-> account/contact creation
-> Stripe Checkout or approved pre-checkout step
-> verified trial/subscription state
-> parent/member access
-> class/library access
-> welcome email
```

Requirements:

- workspace scoped;
- no BNA contamination;
- idempotent signup;
- duplicate email handling;
- consent checkbox;
- privacy/terms links;
- timezone;
- child/minor data minimized;
- clear trial and post-trial price;
- success/cancel states;
- no access based only on client redirect;
- server verifies Stripe state;
- email failure does not lose payment/access state;
- support recovery.

If live Stripe checkout is blocked by a real unresolved policy, deploy the complete safe flow in test mode and show a precise internal blocker. Do not leave a dead CTA.

---

# 19. PHASE P — TESTING

## 19.1 Asset tests

- all candidate files accounted for;
- ZIP counts reconcile;
- exact duplicates grouped;
- no originals overwritten;
- public exports have no GPS metadata;
- no unapproved minor asset in public paths;
- image dimensions and file sizes within budgets;
- all public images referenced by manifest;
- no broken asset URL.

## 19.2 Public funnel tests

Viewports:

```text
390x844
768x1024
1440x900
```

Test:

- countdown;
- campaign expiration;
- hero Vimeo playback;
- poster fallback;
- reduced motion;
- navigation;
- CTA;
- signup;
- slideshow;
- TorahAnytime link;
- founder image;
- community avatar;
- press strip;
- legal footer;
- no TEST/review links;
- no horizontal overflow;
- keyboard;
- contrast;
- accessible names.

## 19.3 Vimeo tests

- hero ID correct;
- lesson ID correct;
- production domain allowed;
- no CAPTCHA route;
- CSP permits Vimeo;
- error fallback;
- mobile autoplay fallback;
- reduced motion;
- captions state;
- no raw MP4 public exposure.

## 19.4 Email tests

- readiness diagnostics;
- sender/domain status;
- template render;
- webhook signature;
- idempotency;
- bounce/complaint handling;
- single approved test send or exact blocked reason;
- no bulk send;
- no secret leakage.

## 19.5 Stripe tests

- test checkout;
- 30-day trial;
- $67 recurring price;
- webhook verification;
- duplicate events;
- success/cancel;
- failed payment;
- cancellation;
- access grant/revoke policy;
- workspace isolation;
- no live charge.

## 19.6 View-as tests

Run all tests from Section 15.6 at API and Playwright level.

## 19.7 Full verification

Run as applicable:

```text
npm test
npm run bna:run:validate
node scripts/audit-secrets.mjs
route/action watchdogs
focused Playwright tests
live app smoke
Vimeo live smoke
email readiness/test smoke
Stripe test-mode smoke
public privacy smoke
workspace isolation smoke
```

Record exactly what passed, failed, skipped, or remained blocked.

---

# 20. DEPLOYMENT AND LIVE PROOF

After focused tests:

1. Review scoped diff.
2. Reject unrelated changes.
3. Commit only this requirement scope.
4. Push to PR #5 or the correct successor branch.
5. Do not merge.
6. Deploy through the approved Railway/release path.
7. Verify the deployed commit.
8. Run live smokes.
9. Capture redacted screenshots.
10. Record deployment ID and status.
11. Leave real external blockers open.

No claim of “done” without live proof for:

- public Vimeo hero;
- public funnel;
- view-as route;
- deployed asset URLs;
- integration readiness UI.

Stripe does not require a real charge as proof.
Email does not require a bulk send as proof.

---

# 21. REQUIRED FINAL RESPONSE TO SHLOIMIE

Return a concise operator report with:

## A. Asset library

- source folders and ZIPs found;
- total images/videos;
- duplicates;
- rejected/private items;
- local organized root;
- contact-sheet link/path;
- manifest path.

## B. Recommended images

A table with:

```text
rank
thumbnail/contact-sheet reference
organized local path
public derivative path
intended use
crop
rights status
reason
```

Include the hero, locations slideshow, founder portrait, and community avatar.

## C. Funnel

- final headline;
- subhead;
- CTA;
- campaign timer deadline/status;
- live URL;
- screenshot links.

## D. Vimeo

- hero video ID;
- lesson video ID;
- token/account readiness;
- live playback result;
- blocked actions.

## E. Email

- provider;
- domain;
- sender;
- readiness;
- test-send result or exact blocker;
- no secret values.

## F. Stripe

- account/mode;
- product;
- price;
- trial;
- webhook;
- checkout test result;
- live readiness;
- unresolved financial Decisions;
- confirm no live charge occurred.

## G. View as Rabbi

- exact super-admin entry route/button;
- target role;
- read-only status;
- audit behavior;
- tested scope;
- screenshot of Rabbi view;
- exit behavior.

## H. Git/release

- files changed;
- tests;
- commit;
- PR;
- deployment;
- live-smoke evidence;
- remaining blockers;
- exact next command.

Do not send a vague summary such as “assets organized” or “integrations ready.”
Give exact paths and proof.

---

# 22. DEFINITION OF DONE

This goal is complete only when:

1. Every relevant new Downloads asset and archive is inventoried.
2. Originals are preserved.
3. Exact and near duplicates are grouped.
4. Images/videos are meaningfully titled and classified.
5. A local organized One Time asset library exists in an obvious location.
6. Contact sheets and manifests exist.
7. Rights/privacy state is recorded.
8. Ranked recommendations exist.
9. Approved derivatives are organized in the repo.
10. The landing uses the selected safe assets.
11. The public copy is a simple worldwide Mishnayos signup funnel.
12. The countdown uses a real, non-resetting deadline.
13. The correct Vimeo promo video plays live in the hero.
14. The lesson video embeds inside the classroom without CAPTCHA.
15. Email is configured to the highest safe live state, with one approved test send or an exact blocker.
16. Stripe is configured to the highest safe live-ready state, with test checkout and no real charge.
17. Shloimie can securely enter “View as Rabbi” from the One Time workspace.
18. View-as is server-authorized, audited, read-only by default, branded, and exact in scope.
19. Rabbi sees only One Time-relevant backend modules and data.
20. BNA, Webcraft, Codex, Watchdog, raw intake, and developer internals do not leak into the Rabbi experience.
21. Public TEST/review links are removed.
22. Related broken classroom/resource/email/integration controls are fixed.
23. Action and route registries are updated.
24. Focused tests, full tests, and privacy/workspace tests run.
25. Scoped commit/push/deployment/live proof exists.
26. No secrets or private minor assets are committed.
27. No real Stripe charge, bulk email, destructive Vimeo action, PR merge, or unapproved media publication occurs.
28. Remaining blockers are concise Decisions with:
   - owner;
   - missing information;
   - recommendation;
   - alternatives;
   - consequence;
   - exact next action.

---

# FINAL COMMAND

Treat this as a goal-mode continuation.

First organize and understand the Downloads assets completely.
Then use the best approved assets.
Then simplify the funnel.
Then make Vimeo load live.
Then complete email and Stripe readiness.
Then implement secure super-admin “View as Rabbi.”
Then verify, push, deploy safely, and report proof.

Do not stop after an inventory.
Do not stop after a design document.
Do not create another queue.
Do not expose credentials.
Do not publish unapproved minors.
Do not use a fake countdown.
Do not claim completion without live evidence.
